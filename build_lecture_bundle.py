#!/usr/bin/env python3
"""Build the question + answer + lecture-notes bundle.

The script is intentionally dependency-free. It creates intermediate files under
``lecture_work/`` so automatic matching, manual review, rendering, and checking
are repeatable.
"""

from __future__ import annotations

import argparse
import json
import math
import re
import sys
from collections import Counter, defaultdict
from dataclasses import dataclass
from pathlib import Path
from typing import Any


ROOT = Path(__file__).resolve().parent
SOURCE_MD = ROOT / "2026刷题班_题目与答案合并版.md"
OUTPUT_MD = ROOT / "2026刷题班_题目+答案+讲解合并版.md"
AUDIO_DIR = ROOT / "audio"
TRANSCRIPTS_DIR = ROOT / "transcripts"
WORK_DIR = ROOT / "lecture_work"

QUESTIONS_JSON = WORK_DIR / "questions.json"
TRANSCRIPTS_JSON = WORK_DIR / "transcripts.json"
ALIGNMENT_JSON = WORK_DIR / "alignment_candidates.json"
REVIEW_JSON = WORK_DIR / "review_summaries.json"
REVIEW_MD = WORK_DIR / "alignment_review.md"
STUDENT_REPORT_MD = WORK_DIR / "student_quality_report.md"


R_ID_RE = re.compile(r"(R\d{8}-\d{6})")
META_PAGE_RE = re.compile(r"题目所在页\*\*：第\s*(\d+)\s*页")
QUESTION_HEADING_RE = re.compile(r"^##\s+(.+)$")
CHAPTER_HEADING_RE = re.compile(r"^#\s+(?!#)(.+)$")
SPEAKER_RE = re.compile(r"^\s*\d+号讲话人\s+\d{2}:\d{2}:\d{2}\s*$")
TIMESTAMP_RE = re.compile(r"\d+号讲话人\s+\d{2}:\d{2}:\d{2}")
PUNCT_RE = re.compile(r"[\s，。、“”‘’：:；;！？!?（）()\[\]【】《》<>—\-_.|/\\,`~@#$%^&*+=]+")
SECTION_RE = re.compile(r"^#")


CHINESE_NUMS = {
    "零": 0,
    "一": 1,
    "二": 2,
    "两": 2,
    "三": 3,
    "四": 4,
    "五": 5,
    "六": 6,
    "七": 7,
    "八": 8,
    "九": 9,
    "十": 10,
    "百": 100,
}

STOP_TERMS = {
    "给定资料",
    "给定材料",
    "请结合",
    "请根据",
    "谈谈",
    "理解",
    "认识",
    "分析",
    "进行",
    "归纳概括",
    "提出",
    "建议",
    "对策",
    "参考答案",
    "材料",
    "题目",
    "要求",
    "分",
    "第",
    "页",
}

ASR_NOISE_TERMS = {
    "好多人",
    "感觉我",
    "一天天",
    "同学会问",
    "屏蔽掉",
    "新疆",
    "上厕所",
    "吃饭",
    "休息",
    "管理员",
    "刷视频",
    "哈哈",
    "我先去",
    "不好过",
}

LECTURE_PREFIXES = ("审题定位", "材料拆解", "采点思路", "答案组织", "易错提醒")


DETAILED_LECTURE_BULLETS: dict[str, list[str]] = {
    "S1-Q02": [
        "课堂起点：老师先把这题放在江苏归纳概括成效题里看。题干要求“请用一段话”，所以不能把答案写成散点清单；作答前要先想清楚总领句、分类角度和句间衔接，最后形成“总括句+同层分类+核心成效”的一段话。",
        "审题总开关：这题真正要概括的是“显著成效”。“智慧执法方面的创新举措”是直接修饰语，也就是帮助定位材料的做法，不能完全不写，但只能高度概括；主要篇幅必须写这些举措带来的结果。地方和部门本身不是采分重点，不要被“行政行为码、智慧交通运输管理系统、热力图、AI 小秘书”等名称牵着走。",
        "筛选规则：凡是系统如何开发、由哪些模块构成、怎样运行、覆盖到哪里、如何培训推广，通常属于做法；凡是体现效率提高、监督加强、风险降低、流程规范、秩序维护、精准查处、群众或执法人员更方便的内容，才是成效。材料中出现“推动、提高、有效、从源头上、保证、支撑、提供依据、弥补”等结果性表达时，要优先判断为采分信息。",
        "生成路径：每则材料都按“原文表述—筛选成效—合并同类项—提炼概括词—放入一段话”的顺序处理。不要先背一个规范词再硬套材料，而是先从原文结果句里找可用表达，再把零散成效合并成同一层级的分类词。",
        "材料一：行政行为码的定义、赋码规则、覆盖范围、掌上执法端操作、专题培训和推广目标，主要说明这项创新举措如何建立和推进。真正进入答案的是结果：当事人可查询文书和流程，执法人员可回溯案件办理，全流程信息清楚；后文又给出更规范的成效句，即推动执法权力全流程在线运行、及时预警、精准监督、风险可控、留痕可溯。由这些结果合并提炼出“让执法阳光高效”。",
        "材料二：大屏提示、源头数据采集、不停车称重、语音弹窗和手机 App 通知，都在说明智慧交通运输管理系统的运行原理。成效要抓“目的实现”和“结果变化”：为提升超载超限执法能力而研发，科技赋能提升管理效能，从源头上消除超载超限行为，节约检查时间并增加驾驶员收入；“从想超变成不敢超、不愿超”可回扣到源头治理。合并后分类为“提升执法能力”。",
        "材料三：人群聚集热力图、5G 视频巡逻、防走失系统是借助互联网、大数据技术防控的三个小做法。对应成效分别是为安保指挥决策提供科学依据、弥补固定监控缺陷、找寻走失儿童和走散游客；开头“未发生重大刑事治安案件、未发生踩踏事故、社会治安秩序良好”虽有铺垫作用，但也是总效果。把这些合并，分类词就是“维护社会治安秩序”。",
        "材料四：AI 小秘书的企业信息协查、报告助手、政策法规问答，是生态环境移动执法智慧助手的三个功能做法。筛选成效时看功能之后的结果句：企业信息协查提高日常执法效率，报告助手保证执法记录规范性和准确性，政策法规问答为环境违法案件精准查处提供可靠支撑。由“用于生态环境移动执法的智慧助手”提炼出“助力移动执法”。",
        "归并方法：四则材料不能机械罗列为“行政行为码、系统、热力图、AI 小秘书”，因为这些多是创新举措本身；要用老师说的“在什么方面”的角度串起来：在阳光高效执法方面、在提升执法能力方面、在维护社会治安秩序方面、在移动执法方面。这样分类词来自材料结果，又能突出题干要求的“显著成效”。",
        "答案组织：总领域优先从能统领全文的材料首段找，找不到就回到题干。本题可从题干提炼总领句“智慧执法新举措成效显著”，后面顺次写四个方面。每个方面先放概括词，再接最规范、最能概括成效的原文或近原文表达，系统构成、运行流程和背景问题只作为定位依据，不作为答案主体。",
        "易错提醒：第一，不能把题目写成智慧执法工具清单，成效才是主干；第二，不能把研发、构成、覆盖、培训、流程等做法大量搬进答案；第三，不能见到材料前面的铺垫场景就照抄，要判断其中是否有结果句；第四，江苏题一般不需要背所谓规范词，材料里往往已经给出可用表述，关键是把零散结果合并成同一层级的采分点。",
    ],
    "S1-Q03": [
        "课堂起点：这题仍是归纳概括成效题，问的是一些地方采取新举措促进乡村产业发展取得的可喜成效。作答不能只写“水稻+禾虫”“农业大托管”“民宿集群”“戏曲小镇”这些新举措名称，而要写新举措已经带来的产业发展结果。",
        "审题总开关：题干中的“新举措”是修饰成效的做法线索，“乡村产业发展”提示分类标准，“可喜成效”才是采分主体。四则材料没有统一总括段，总领句可以从题干提炼为“新举措促进乡村产业发展”。",
        "筛选规则：介绍背景、人物故事、模式构成、准备推广、明年争取等内容，多数只是定位做法或目标；已经发生的产量提高、收入增加、群众接受、返乡就业、旅游变热、文化传承、道路风景改善、特产有销路，才是成效。老师特别提醒，将来“争取让更多村民腰包鼓起来”属于目标，不能当作已经实现的成效。",
        "材料一：第一段是引出“水稻+禾虫”绿色生态种养模式，县里配农技专家一对一指导是新举措的做法支撑。真正要写的成效是禾虫减少水稻病害、提供优质有机肥料，使大米质优价高、供不应求，并通过禾虫收益增加农民收入。由“绿色生态种养模式”提炼分类为“绿色生态发展”。",
        "材料二：“大托管”本质上是集约化农业生产新模式，统一耕、种、防、收、售等是做法。成效要抓已有变化：小麦、玉米丰收后给农民分红，玉米大丰收体现产量提高，邻村羡慕并签协议体现示范效应，越来越多村民接受体现群众思想转变。分类词可提炼为“集约化农业生产发展”。",
        "材料三：何大姐回村、土坯院子、品牌民宿和旅行体验，都是为了说明废弃村落被打造成民宿集群。成效不是装修细节，而是形成民宿品牌、增加旅行者体验、订单火热并成为旅行热门目的地；同时吸引村民返乡，带动路线规划师、咖啡师、服务人员、小卖部等就业，村庄重新热闹起来。分类词是“旅游业发展”。",
        "材料四：建立博物馆、传习所，成立戏曲文创工坊、戏曲民宿、酒坊等小做法，合起来是打造戏曲小镇。成效包括游客慕名而来、乡村旅游火了，平调落子得到传承并培养人才，村子打响名声，道路通畅、风景更美，民宿农家乐和土特产销售有机会，最后落到生活的底气和希望。分类词是“文化产业发展”。",
        "归并方法：四则材料按产业领域归并，而不是按人物或故事归并：绿色生态发展、集约化农业生产发展、旅游业发展、文化产业发展。每一类都要保留“新举措+成效”的关系，但篇幅重点放在成效上。",
        "答案组织：一段话可写成“新举措促进乡村产业发展。一是在……方面，采取……举措，带来……成效；二是在……方面……”。分类短语保持同一颗粒度，后面的成效尽量用材料中的连续结果句和近原文表达。",
        "易错提醒：第一，不要把“稻田养虫”“分红仪式”“何大姐返乡”“蒋大娘饭馆”当作答案主体，这些多是引入场景；第二，不要把计划推广、明年争取等目标写成已然成效；第三，不要把材料三、材料四都简单写成旅游，材料三主线是民宿集群带动旅游业，材料四主线是戏曲小镇带动文化产业。",
    ],
    "S1-Q04": [
        "课堂起点：这题问“好做法所产生的积极效应”，仍然是成效题。好做法要写，但只能高度概括；真正展开的是这些做法带来的管理、服务、治理、守法等积极变化。",
        "审题总开关：题干限定为“执法工作的一些好做法”，材料里一共有五个具体操作：定制专属便民市场、“摊有序”数字化应用场景、直播式垃圾分类执法行动、对轻微违法不予实施行政强制措施、信用修复代办。老师最后把五个操作归并成四个主题：精细化管理、创新执法形式、柔性监管、贴心服务。",
        "筛选规则：做法的名称、运行条件、注册数量、积分管理、直播流程、专班专组等是定位依据；“合法经营、满足需求、投诉下降、治理有温情、认识危害、增长知识、自觉行动、有精度有温度、软环境、解决困难、拉近感情、降低难度”等结果句，才是要进入答案的积极效应。",
        "材料一第一层：定制专属便民市场是精细化管理的一个好做法。以前摊贩没证照、没固定摊位、和其他摊主争吵、听到城管心里发毛；现在有固定地盘、名正言顺经营、商户有发言权并满意。由前后对比筛出成效：让流动摊贩合法经营，满足群众和商户的实际需求。",
        "材料一第二层：“摊有序”数字化应用场景也是精细化管理。预约摊位、打卡、监督投诉、积分制、红黑榜、监控查看等是做法；实施半年后占道经营投诉量从月均42件降到5件，这是明确成效。后文“微心愿、暖心摊位、共富摊商贷、摊贩安身险”仍是小做法，合并出的效果是城市治理更有温情，执法人员和流动商贩从对立走向并肩前行。",
        "材料二：直播式垃圾分类执法行动的主题是创新执法形式。直播检查、现场讲解、导师参与、抽奖互动都是活动内容；真正的积极效应是让市民认识乱投放垃圾的危害，增长垃圾分类知识，并推动垃圾分类成为自觉行动。80多万网友围观、30多万次点赞可辅助说明形式新、参与度高，但不宜替代核心成效。",
        "材料三第一层：对情节显著轻微或没有明显社会危害的违法行为不予实施行政强制措施，可概括为柔性监管。企业容易因无心小错受罚、被记入失信名单，是背景和问题；成效要写材料里的规范表述：让执法既有精度也有温度，实现约束和规制作用，助力打造营商法治软环境。",
        "材料三第二层：信用修复代办是贴心服务。符合条件、提供材料、代办手续、设立专班专组等是做法；成效是解决企业运营困难，增强企业依法经营意识，拉近政企感情，并降低执法工作难度。材料里“起死回生”要回到前文，理解为解决不良信息导致的运营困难。",
        "归并方法：不要按三则材料机械写三点，材料一和材料三都需要拆分。材料一两个做法同属“精细化管理”，材料三两个做法分别对应“柔性监管”和“贴心服务”；最后形成四个同层分类，才和参考答案的颗粒度一致。",
        "答案组织：总领句可写“好做法提升管理服务水平”。后面用“在精细化管理方面、在创新执法形式方面、在柔性监管方面、在贴心服务方面”顺次展开，每个方面先写高度概括的好做法，再接对应积极效应。",
        "易错提醒：第一，不要只堆“摊有序、直播、清单、信用修复”等做法名称；第二，不要把微心愿、暖心摊位、贷款保险逐项铺开，要合并为治理更有温情；第三，直播中关于地沟油、污染的内容主要是垃圾分类知识，不要偏离到单独论证垃圾处理；第四，材料三的背景问题不能写成答案主体。",
    ],
    "S1-Q05": [
        "课堂起点：这题继续问“好的做法产生的积极效应”，但难点比上一题更大，因为三则材料里实际有六个相对独立的做法。作答时先保证把六个做法及其成效找全，再考虑能不能进一步归类。",
        "审题总开关：题干要求概括积极效应，“这些做法”是修饰词，也要高度概括。材料一首段有总括性提示，反复说法治支撑、法治护航、法治保障，所以总领句可概括为“执法工作引领法治建设”。",
        "筛选规则：活动流程、参与人数、座谈会、师傅带徒弟过程、律师驻队形式、专家把脉方式、媒体宣传、无人机巡逻等多是做法；普法、增长知识、了解执法、年轻人成长、减少冲突、提升执行到位率、精准整改、震慑虚假宣传、填补盲区、倒逼守法、立体防控、节省警力、规避风险，才是积极效应。",
        "材料一第一层：综合执法开放日是第一个做法。模拟演练、普法展板、知识竞赛、发放宣传袋、座谈会、随队上街执法都是活动内容；成效要抓三句：给市民上现场普法课，让市民增长知识，让更多市民走进并了解执法工作，营造城市管理工作环境。可归到“百姓福祉”或“面向市民普法”。",
        "材料一第二层：导师帮带制是第二个做法。配备导师、师傅带徒弟、手绘杨梅地图、帮摊贩搬运等是过程；老师特别强调，设身处地为群众着想是小王受到的启发，不宜单独写成制度成效。制度的核心成效是帮助年轻执法人员在实践中锻炼成长，可归到“队伍建设”。",
        "材料二第一层：驻队律师是第三个做法。值班、随队执法、随队普法、文书审查、专题培训是做法展开；材料后面直接给出成效：减少因执法行为产生的冲突，提升罚款执行到位率。它指向执法行为本身，归到“执法手段”。",
        "材料二第二层：环保医生是第四个做法。专家查资料、看现场、列清单、开药、电话座谈等都是会诊把脉的方式；真正的成效是为环保部门查处环境违法行为提供重要参考，指导企业精准整改。老师提醒“精准”是这一点的核心词。它可归到“社会发展”或“企业治理”。",
        "材料三第一层：查办典型案件是第五个做法。调查、处罚、责令整改、媒体宣传、提醒消费者举报投诉都属于多措并举；材料开头“查办一起，震慑一片”已经给出做法和成效关系，答案应概括为查办典型案件，震慑虚假宣传行为。",
        "材料三第二层：无人机抓拍是第六个做法。对违停、打电话、不系安全带等违法行为抓拍，以及疫情防控中的空中巡逻、宣传、劝返、监控、投放物资，都是无人机执法的具体运用；成效包括填补监控盲区、倒逼市民自觉守法、打造空中加地面的立体防控模式、节省警力、规避高风险面对面接触。",
        "归并方法：参考答案把六个做法重组为四类：开放日对应百姓福祉，导师帮带制对应队伍建设，驻队律师和无人机对应执法手段，环保医生和查办典型案件对应社会发展。老师也提醒，考场上如果短时间难以重组，按六个做法顺序写也有可操作性，关键是不要漏掉成效。",
        "答案组织：高配写法是“执法工作引领法治建设。在执法手段上……在队伍建设上……在社会发展上……在百姓福祉上……”。稳妥写法是按材料顺序压缩六个做法，每个做法后紧跟一到两个核心成效，保证一段话内部层次清楚。",
        "易错提醒：第一，不要把六个故事当成热闹案例复述；第二，导师帮带制的成效落在年轻人成长，不是直接写群众配合；第三，“执法效果显著”不是具体得分点，要展开为填补盲区、倒逼守法等结果；第四，材料三的两个故事不要混成一个点，查办典型案件和无人机抓拍的成效不同。",
    ],
    "S1-Q06": [
        "课堂起点：这题不是单纯概括成效，而是要求把江苏科普活动在“活动针对性、活动方式”上的特色和“成效显著”一起概括出来。每个材料都要同时看三层：在哪个领域开展科普、用什么特色方式或针对谁、产生了什么成效。",
        "审题总开关：题干已经提示分类可围绕科普活动展开，但材料里的分类词需要从领域中提炼。老师最后明确为四个领域：科学教育、天文观测、农业生产、阵地建设。总领句可写“特色科普活动取得显著成效”。",
        "筛选规则：活动名称、举办单位、参赛人数、专家团队、参观流程、PPT授课、田间指导、科普馆名称等多是活动方式或活动内容；丰富知识、传承文化、培育素养、激发兴趣、体验乐趣、做强产业、落实成果、消除误区、爱粮节粮、理念深入人心，才是成效。题干要求方式和针对性，所以做法不能全删，但要为成效服务。",
        "材料一：前面诗词题目是引出“诗词里的科学”网络挑战赛。活动方式是用诗词引导青少年寻找科学知识，属于国学与科学结合；成效是丰富知识储备，传承中华优秀传统文化，培育科学精神和人文素养，并为青少年的科学梦想助力。分类词来自结尾的科学教育，可写“在科学教育上”。",
        "材料二：走进紫金山天文台是科普研学活动，活动方式主要是资深讲解员和科普专家指导，配合观察实物、聆听讲解、提问互动、使用望远镜观测。成效包括感受古代天文学智慧，了解近代天文学发展，激发外太空兴趣，体验“小小天文观测家”的乐趣。分类词是“天文观测”。",
        "材料三：农业技术科普培训活动的方式是图文并茂授课、深入浅出讲解和田间地头实地指导。第三段学员说“坐得住、听得进、学得会”说明方式有效，最终成效集中在负责人总结中：让特色产业强起来、生产要素活起来、科技成果落下去，持续支撑农业科技与生产服务。分类词是“农业生产”。",
        "材料四第一层：科普大学突出活动针对性。社区老年居民科普渠道有限但需求大，所以针对居民实际需求整合资源，开办健康科普。成效要回到付奶奶的话：以前有健康保健认识误区，现在增加了解，能更从容应对生活所需，可概括为消除健康保健误区。",
        "材料四第二层：微科普馆突出阵地建设。爱粮节粮科普教育基地让孩子从“五谷不分”到有切身感受并懂得珍惜粮食，可概括为推动爱粮节粮；低碳科学馆使绿色环保理念深入人心，石头城印记馆让历史走进现实，多个微科普馆共同促进科普工作在“最后一公里”“全域开花”。",
        "归并方法：四则材料不能只按活动名称罗列，也不能只写成效。应采用“领域+特色方式/针对性+成效”的结构：科学教育靠诗词方式，天文观测靠专家指导，农业生产靠图文授课和实地指导，阵地建设靠针对居民需求的科普大学和家门口微科普馆。",
        "答案组织：一段话可写成“特色科普活动取得显著成效。一是在科学教育上……二是在天文观测上……三是在农业生产上……四是在阵地建设上……”。每个分句先点出领域，再压缩活动方式或针对性，最后接最核心成效。",
        "易错提醒：第一，不要只写“网络挑战赛、研学活动、培训活动、科普大学”而漏掉题干要求的特色和成效；第二，“引导青少年认识客观世界”“观察实物、聆听讲解”主要是活动方式，不要误当成全部成效；第三，材料四有两个活动层次，科普大学和微科普馆都要照顾到；第四，接待人数、覆盖学校数等规模信息可辅助理解，但不是本题最核心的采分点。",
    ],
    "S1-Q08": [
        "课堂起点：这题表面也是成效题，但难点在“借鉴传统文化中的相关智慧赋能现代社会治理”。老师强调，归纳的是成效，同时要概括借鉴了什么传统智慧；题干又明确说“在不同领域”，所以分类要按领域来。",
        "审题总开关：这里的做法链条比较特殊，A做法是借鉴传统文化智慧，B做法是现代治理中的具体运用。B单独看可能是做法，但相对于A而言就是成效。比如全面推行新时代河湖长制，单独看是制度做法，但相对于借鉴古代河长制，就是借鉴传统智慧产生的现代治理成效。",
        "筛选规则：先找“借鉴了什么智慧”，再找“落到哪个现代治理领域”，最后找“产生了什么结果”。故事背景、历史典故、人物身份、诗句来源主要用于定位传统智慧；化解纠纷、制度推行、水质改善、妥善处置、生态宣传、文明习惯提升、公共卫生资源保障等才是成效。",
        "材料一：传统智慧可合并为“六尺巷”故事和“和为贵”智慧，本质都是礼让、和谐、调解。现代领域是基层社会治理；成效是化解土地纠纷和邻里纠纷，并通过纠纷减少反映民风改善。不要被张英身份和六尺巷典故本身带偏，答案要落到现代治理结果。",
        "材料二：传统智慧是古代河长、渠长、湖长等治水制度，现代运用是全面推行新时代河湖长制。领域是制度建设；成效既包括这一制度在新时代落地，也包括河长上岗后河畅水清、水质由劣V类变为III类，村民困扰解决，生活环境改善。",
        "材料三：传统智慧是儒家文化中保护生态的理念和做法。领域是生态保护；现代成效包括妥善处理亚洲象北移这一野生动物突发事件，加大生态保护宣传，激发人与自然和谐相处的基因。材料最后“全方位考查和检验”较笼统，具体要落回这三层结果。",
        "材料四第一层：借鉴耕读传统，现代运用是推广全民阅读、探索书香城市建设新路径。领域是文明实践；成效是阅读活动改变村民不良习惯，提升文明习惯，使阅读成为传播思想、成就梦想的文明实践新路径。",
        "材料四第二层：疫情防控部分借鉴的是一方有难、八方支援、团结一心、共克时艰的优良传统，以及天下为公、先集体后个人的文化自觉。领域可写公共卫生领域或公共卫生危机应对；成效是保障医疗资源和物资供应，打赢疫情防控人民战争，在自身承压下为国际社会提供援助，服务构建人类命运共同体。",
        "归并方法：这题四则材料实际可以归成五个领域：基层社会治理、制度建设、生态保护、文明实践、公共卫生。材料四包含两个不同传统智慧和两个现代治理方向，不能强行并成一个点。",
        "答案组织：总领句可写“传统文化赋能现代社会治理”。每个分句用“在某领域，借鉴某传统智慧，产生某成效”的顺序展开。这样既回应“传统文化智慧”，又回应“不同领域”和“显著成效”。",
        "易错提醒：第一，不要只写传统典故，必须写现代治理结果；第二，不要只写现代做法而漏掉借鉴的传统智慧；第三，看到“全面推行河湖长制、探索书香城市建设新路径、加大生态保护宣传”这类现代做法时，要结合题干判断它们是否也是借鉴智慧后的成效；第四，材料四不要漏掉公共卫生领域。",
    ],
    "S1-Q09": [
        "课堂起点：这题题干明确说“花式普法的做法及其成效”，做法和成效都要写，不是只写结果。和前面成效题不同，这里要先概括“花式”做法，再紧跟这些做法产生的普法、治理和依法行政效果。",
        "审题总开关：材料第一段说依法行政深入推进、普法打开方式越来越“潮”，所以总领句可概括为“普法新方式推进依法行政”。后面分类可按领域来：材料一是民生领域，材料二是基层治理领域，材料三是生态环境领域。",
        "筛选规则：直播话术、流动集市、互动区、桌贴漫画、普法清单、人偶漫画微视频、评弹演出等是“花式”做法；增长法律知识、认识违法危害、提高反诈意识、减少酒后滋事、形成下单共识、引导守法用法、扩大影响力、让群众理解生态环境违法后果，才是成效。",
        "材料一第一层：直播带货式普法的做法是借用直播带货的话术和场景讲法律。吸引观众、冲上热搜、播放量高可以辅助说明形式受欢迎，但核心成效是展示盗窃、吸毒等违法行为危害，让法律知识进入观众脑袋，增长法律知识。",
        "材料一第二层：警营开放日以流动集市形式设置反诈、食药安全、交通安全、长江大保护、防范非法集资等宣传防范互动区。这个做法可概括为宣传安全防范常识；成效是提高群众反诈意识和识诈能力，也可扩展为提升安全防范意识。",
        "材料一第三层：温馨提示桌贴和宣传画是当地警方设计的普法形式，用漫画、小诗、提示语默默提醒消费者。成效是起到引导和提醒作用，减少酒后滋事，让店铺经营更安全、更顺畅，可概括为实现安全经营。",
        "材料二第一层：订单式普法的做法已经由材料概括好，即群众根据需求下单，司法部门优化供给、送法上门。成效包括帮助小区业委会顺利成立，实现普法工作有声有色、有形有效，并使“有需求，下订单”成为群众普遍共识。不要把派发资料数量当作核心成效。",
        "材料二第二层：小叽说法是具有地方特色的动漫普法品牌，具体形式有人偶互动、漫画、动漫微视频、普法小游戏和衍生品。成效是扩大品牌辐射力和影响力，引导群众自觉守法、遇事找法、解决问题靠法。",
        "材料三：评弹普法的做法是将生态环境损害赔偿典型案例以地方特色评弹形式展现给群众。成效包括形象再现生态环境违法案件查处和当事人承担法律后果的过程，解决专业宣讲听不懂的问题，让普通群众更容易理解和接受。",
        "归并方法：高配分类是三类：民生领域包含直播带货式普法、警营开放日、温馨提示桌贴；基层治理领域包含订单式普法和小叽说法；生态环境领域包含评弹普法。老师也提醒，若考场觉得领域分类太难，可以按六个做法顺序写，但要把基层治理、生态环境、依法行政等关键词补进去。",
        "答案组织：一段话可写成“普法新方式推进依法行政。一是在民生领域……二是在基层治理领域……三是在生态环境领域……”。每个领域内按“做法+成效”成对组织，避免只列活动名。",
        "易错提醒：第一，不要把播放量、围观人数、演出场次等热闹程度当作主要采分点；第二，不要把订单式普法后面的清单、资料、场次铺得太细，因为“订单式普法”已经概括了小做法；第三，材料三必须点出生态环境违法案件和法律后果，否则评弹形式就失去题目中的治理指向。",
    ],
    "S1-Q10": [
        "课堂起点：老师先提醒，这题题干已经把结构交代清楚：阅读活动中有一些新现象，不同群体采用不同方式，并取得相应收获。因此分类不能随意按材料场景或技术平台切，而要围绕“不同群体”来组织。",
        "审题总开关：题目要求归纳概括的是“不同群体—不同方式—相应收获”这一组关系。“新现象”主要提示阅读方式变多、阅读场景变新；作答时方式要概括进来，但重点仍是这些方式给各类群体带来的收获。",
        "筛选规则：群体身份、阅读方式、实际收获是核心采点；软件硬件构成、馆内设施、活动流程、接待读者人数、法规制度等多是背景或做法。凡是体现阅读更便利、文化体验更丰富、特殊需求被满足、学习习惯形成、阅读兴趣被激发、碎片时间更有价值的内容，才优先进入答案。",
        "材料一：群体是阅读障碍者，包括盲人、低视力者、听力障碍者等。无障碍软硬件、无障碍电影、视障读者阅览室、有声读物和相关条约，是他们采用或获得的阅读方式支撑；成效要写便利获得文化产品，满足特殊阅读需求，参与文化体验和交流，提升社会融合，保障阅读权益并增强幸福感。",
        "材料二：群体是青少年，方式是通过文博场馆、博物馆、数字藏经洞和文创载体开展跨界阅读。材料中的榜单、人次、文创产品说明阅读场景变新，但作答重点是收获：实现阅读跨界和延伸，触摸历史心跳，感受文化气息，让阅读支脉继续延伸。",
        "材料三：群体可合并为城乡居民，既包括乡村老人、村民，也包括城市居民。有声图书馆和城市书房是两类方式；对应收获是破解老人阅读难题，使定时集中学习变为便捷自主学习，帮助居民养成学习习惯，拉近人与阅读、学习的关系，改变日常生活并提升阅读体验。老师特别提示，关于城市书房的法规建设是政府治理成果，不是本题群体收获的主干。",
        "材料四：群体是互联网用户，方式是数字阅读，包括有声特色航班、听书 App、播客、短视频读书讲书荐书等。收获包括享受读书快乐，获取新闻热点、高价值话题和信息，让碎片时间更有价值，帮助读者初步了解没读过的书、加深理解读过的书，并激发阅读热情。",
        "归并方法：四组信息按群体归并，形成阅读障碍者、青少年、城乡居民、互联网用户四类。每一类内部再压成“群体+方式+收获”，不要把无障碍设备、博物馆、城市书房、听书软件等并列成没有群体归属的现象清单。",
        "答案组织：总领句可写“多样化阅读方式给不同群体带来收获”。后文用“对于阅读障碍者，利用……；对于青少年，通过……；对于城乡居民，利用……；对于互联网用户，通过……”顺次展开，保证四个分句都同时回应群体、方式和收获。",
        "易错提醒：第一，不要把题目写成阅读活动新现象罗列，题干真正要的是不同群体的收获；第二，不要漏写阅读方式，否则答案少了题干中的“采用不同方式”；第三，不要把接待人数、平台形式、法规制度当作核心收获；第四，材料三不能只写城市书房，也要照顾乡村有声图书馆。"
    ],
    "S1-Q12": [
        "课堂起点：老师先把“新变化”这个抽象问法具体化。这里的新变化不是只找新做法，也不是只找新成效，而是要根据材料同时判断“以前没有、现在出现的做法”和“这些做法带来的效果”。",
        "审题总开关：题干限定为“互联网科技给农村生产生活带来的新变化”。“互联网科技”要高度概括为在线培训、电商、直播带货、智能手机、数字乡村平台等新技术新工具；“农村生产生活”提示分类方向，最后归到农业生产、农产品销售、增收致富、农民生活四个方面。",
        "筛选规则：凡是互联网科技本身、平台板块、直播设备、手机操作步骤等，多是做法或技术支撑；凡是科学种田有新内涵、运销效率提高、收入增加、特色农产品推向全国、线上销售额提升、老人生活更便利、公共服务更好等，是成效。题目问“新变化”，所以做法和成效都可以写，但必须围绕农村生产生活来取舍。",
        "材料一第一层：在线培训、网购农资是“互联网+农业”的浅层对接，云计算、大数据、物联网支撑的智慧农业是深度融合。农田物联 App 和物联网设备可监测耕作层、漏耕、土壤温湿度、光照度和作物长势，这些合起来说明农业生产中的新变化：依托新技术，在线培训和网购农资普及，智慧农业崛起，科学种田有了新内涵。",
        "材料一第二层：小何的故事围绕农产品销售。加硬包装、标准化仓库、自动分拣包装流水线，说明电商推动包装标准化和自动化；运销效率提升、打包工人获得收入、网店订单和复购率稳定，是销售成效；源头分级、产地直发把零散市场需求和产地货源连接起来，平台补贴和拼团资源帮助农户卖得多、卖得远、卖得好。由此归纳为农产品销售方式的新变化。",
        "材料二：直播带货仍属互联网科技在农村中的运用，但它的重心不是普通销售，而是增收致富。小韩从普通销售转为带货主播，说明电商发展发掘新的就业岗位；小杨回乡做带货主播，说明直播带货带动回乡创业；特色面食、牛肉干、大米等被推向全国，线上销售额突破百万元，则说明特色农产品出圈、销售额提升，流量转化为销量。",
        "材料三第一层：老年智能手机课的核心不是“连 Wi-Fi、点微信”这些操作细节，而是智能手机为老人提供基本应用。调取健康码、行程码、微信付款、在线预约挂号等内容，最后落到孙大妈“什么都可以自己弄，不用再烦别人”，体现农民生活更便利。",
        "材料三第二层：数字乡村平台是另一类生活治理场景中的互联网科技。村民在外可随时查看家里情况，有需要时求助邻居和村干部；平台与村干部手机互通，村干部能了解村民诉求、邻里纠纷和种养管理信息。材料结尾的规范表达是核心成效：让农村人全面享受良好公共服务和便利生活。",
        "归并方法：不要按互联网工具硬切成五六点，否则答案会散。老师最后把材料重组为四类：农业生产、农产品销售、增收致富、农民生活。前两类都在材料一，第三类在材料二，第四类在材料三；每类内部再放对应的新做法和新成效。",
        "答案组织：总领句可从材料开头和题干合并为“互联网科技助力乡村振兴”。后文用“在农业生产方面、在农产品销售方面、在增收致富方面、在农民生活方面”顺次展开，每个方面都先点互联网科技带来的新做法，再接最明确的成效。",
        "易错提醒：第一，不要把“新变化”理解得过窄，只写做法或只写结果都不稳；第二，不要把直播间设备、后台数据、点击量点赞数写成核心采点，它们只是说明直播形式；第三，不要把小何雇工、订单量、复购率孤立罗列，要放到农产品销售效率和收入变化里；第四，材料三的智能手机和数字乡村平台可以同归为农民生活，不必拆成两大类。"
    ],
    "S1-Q15": [
        "课堂起点：老师一开始就强调，题干问的是“新现象的特点”，不是让学生复述新现象本身。村超村 BA、线上运动会、运动处方、飞盘等只是现象或做法，必须进一步抽象成特点。",
        "审题总开关：“特点”可以通过“做法+成效”来提炼。先找每则材料里的全民健身新做法，再看这些做法带来的效果，最后从高频词、关键评价词和材料重心中提炼分类词。四则材料最终对应草根化、信息化智能化、科学性、趣味性社交性。",
        "筛选规则：赛事场次、报名人数、点击量、曝光量、平台名称、具体动作和器材等，多是背景或发展情况；能说明全民健身呈现何种趋势的词，如群众主体、乡村体育赛事、线上空间、信息化 AI 智能化、科学运动、运动处方、趣味性和社交性，才是提炼特点的依据。成效部分要辅助说明特点，而不能替代特点。",
        "材料一：村超、村 BA 等“村”字头赛事的做法可概括为开展乡村体育赛事。它的特点来自各行各业草根球员、热情球迷、群众主体、政府到位不越位、地方特色和民族文化融合，所以提炼为“草根化”。对应成效是带动地方农文旅体融合发展和农特产品销售，成为对外宣传名片，让人们重新认识乡村、凝聚发展合力。",
        "材料二第一层：体育场馆暂停开放只是背景，线上空间成为健身展示阵地才是引出点。举办全民健身线上运动会是做法，报名参与、线上竞赛、话题互动等说明健身转到线上；成效是推动竞技体育成果与全民共享。它和后面的自测平台共同指向信息化。",
        "材料二第二层：国家体育锻炼标准线上自测平台利用信息化、AI 智能化手段推行锻炼标准。手机摄像头测试、AI 识别、电子证书等是具体方式；成效是参与更简便、判断更准确、提升测验效果，让大家更有目标地衡量健身效果并激励锻炼，最终概括为实现全民健身服务的均衡性和可及性。因此本则特点是“信息化、智能化”。",
        "材料三第一层：运动人数增加、运动方式多样是背景，问题是不少人缺乏科学运动知识，容易损伤。运动处方是做法，核心特点来自材料反复出现的“科学”：生命在于科学运动、科学运动方式、科学评价方法。成效是把科学运动方式带回家或社区并长期坚持，使运动健身更好惠及患者和健身者。",
        "材料三第二层：八段锦出圈不是为了写传统养生细节，而是说明“运动是良医”理念深入，健身与健康深度融合，健康端口前移。其成效是科学健身成为越来越多群众的自觉选择和全民健身新时尚，进一步支撑“科学性”这一特点。",
        "材料四：飞盘、陆地冲浪、超慢跑、匹克球等小众运动出圈，可概括为发展小众体育运动。它的特点要从材料中的原因词提炼：年轻人对户外体育、健康生活方式有热情，对线下社交有愿望；飞盘门槛不高、容易上手，又有极强的趣味性和社交性。因此特点是“趣味性、社交性”。对应成效是丰富年轻人健身选择，改变生活方式，引导全民健身方向。",
        "归并方法：每则材料先压出现象，再提炼特点，最后保留必要成效。不要把“接地气、有活力、线上运动、运动处方、小众运动”并列成答案，这些颗粒度不一致；要把它们上升为四个同层特点。",
        "答案组织：总领句可写“全民健身呈现新特点”。后文按四个特点展开：“一是草根化……二是信息化、智能化……三是科学性……四是趣味性、社交性……”。每个分句先点特点，再写代表性做法和核心成效，既回应“特点”，又不脱离材料。",
        "易错提醒：第一，不要只写新现象本身，题干要的是特点；第二，不要把游客人数、阅读量、报名数、点赞数当作主要答案，这些多是热度证明；第三，材料三中运动处方和八段锦都服务于科学健身，不宜拆成两个无关特点；第四，材料四的原因和特点容易混在一起，最终要落在趣味性、社交性及其带来的健身选择和生活方式变化。"
    ],
    "S1-Q16": [
        "课堂起点：老师先说明，“理念”属于抽象问法，不能直接把案例做法照搬。做题时仍可用“做法+成效”来反推理念，但最后必须明确写出有几个执法理念，不能只写“你送我检”和“宜停车”做了什么。",
        "审题总开关：题干限定的是“你送我检”和“宜停车”两个基层执法创新案例。材料三整体都围绕“你送我检”，但材料四不是整篇都要概括，前面停车难、违停处罚和举报奖励主要是背景，真正围绕“宜停车”的重点在推出路边临时停车管理体系及后续完善运营模式。",
        "筛选规则：活动名称、检测批次、停车收费细则、违法处罚档次、举报数据、停车难背景等多用于理解案例；能够反映执法价值取向的内容才用于提炼理念，如免费检测、科普咨询、让市民放心、回应群众关注、于法有据、保护监督权、满足短时停车需求、改善秩序、听取意见、从管理者转向服务者。",
        "材料三第一层：食品快检车走进市场，现场设置快检操作台，有送必检、随送随检，免费检测多类食品，并提供食品安全科普咨询服务。第二段市民和摊主都说“放心”，第三段又写对不合格食品及时告知、建议停止食用、抽样核查处置并依法处罚商家，还回应群众最关注的检测项目。由这些做法和成效提炼出的理念是“为民执法”。",
        "材料三第二层：材料特别强调“你送我检”不是拍脑门式创新，而是于法有据，依据是消费者依法享有监督权。三级检测网络、食安笑脸地图、优秀良好一般分级评价等是监管方式；其背后的理念是依法监管，保护消费者法定监督权，并用监管数据和评价体系规范市场。",
        "材料四第一层：宜停车的做法是推出路边临时停车管理体系，施划路边停车泊位，通过停得越久越贵的梯次收费管理调控停车。对应成效是满足市民短时停车需求，释放道路资源，调控交通需求，改善交通秩序。老师提醒，这一层虽然看起来像具体管理办法，但在题目语境中要提炼为“加强管理”的执法理念。",
        "材料四第二层：宜停车推行后出现收费标识不明、缴费流程复杂等争议，于是管理部门广泛听取意见，持续完善运营模式，开通次日续时、采用敬告式执法、减免费用。成效是与民共建共享管理成果，实现从管理者向服务者转变，并赢得更多市民支持。由此提炼为“做好服务”。",
        "归并方法：两个案例各拆两层，不是两个材料各写一个点。你送我检对应为民执法、依法监管；宜停车对应加强管理、做好服务。四个理念都来自材料原文逻辑，不要强行套“规范执法、文明执法、柔性执法”等自己熟悉但材料没有稳定支撑的词。",
        "答案组织：总领句可写“执法创新蕴含执法理念”。后面按“四个理念+案例支撑”组织：一是为民执法，写免费检测、科普咨询、放心和回应关注；二是依法监管，写于法有据、三级检测、食安地图、保护监督权；三是加强管理，写临时停车体系、泊位和梯次收费；四是做好服务，写听取意见、持续完善、共建共享、转变角色。",
        "易错提醒：第一，不要把材料四前四段停车难和处罚数据大量写入答案，它们主要是背景；第二，不要只写“你送我检、宜停车值得推广”，必须提炼理念词；第三，不要把“加强管理”排除在理念之外，命题材料已经用“加强车位管理是一个方面，我们也得做好服务工作”提示了管理与服务并重；第四，创新是两个案例共同属性，不必单列为一个理念。"
    ],
    "S1-Q17": [
        "课堂起点：老师先把这题和第十六题区分开。第十六题问“创新案例”中的执法理念，案例可以包含做法和成效；第十七题问“新举措”中蕴含的服务理念，范围被限定在举措上，所以主要从做法中提炼理念，成效只辅助理解，不宜大段展开。",
        "审题总开关：题干有两个层次。总层次是“新形势、社会发展变局中服务民生”，材料一开头给出总理念：准确识变、科学应变、主动求变；分层次是各地新举措中分别蕴含柔性服务、创新服务、主动服务、为民服务、精细服务。",
        "筛选规则：先找每个故事面对的变局，再找对应的新举措，最后从做法关键词里提炼理念。居民圆梦、治理基础显现、提升化解难题能力、平台成效、节约经费、提高效率、幸福感等，多是效果或辅助说明；开放思维、柔性管理、支援力量、大数据平台、主动作为、直播带货、信息化公示牌、点位长责任制等，才是本题的主采点。",
        "材料一第一组：国际社区的变局是传统基层工作方法在国际社区水土不服。做法包括用开放思维和流利外语打开局面，运用外企管理模式和柔性管理方法，搭建由中外居民组成的协商议事平台；疫情期间又提出“老外+老外”的柔性化服务模式，并建立社区、业委会、物业联防联动机制。材料中“柔性”反复出现，理念提炼为“柔性服务”。",
        "材料一第二组：九龙新寓社区面对的是疫情期间工作量陡增、人手不够、服务手段落后。做法不是简单说基层辛苦，而是引入区街干部等支援力量，推出“彩虹家园”大数据服务平台，并形成“大数据赋能铁脚板”的工作方式。老师提醒，四个“1分钟”是平台功能，不要单独拆成精准服务；整体关键词是从落后到创新，所以理念是“创新服务”。",
        "材料二第一组：临湖区软件企业面对疫情冲击下人才返岗难、项目交付风险和企业短期困境。人社局的关键词是“主动作为”，通过召开会议、献计献策、充分论证、合作开发，搭建“神兵点将”人才共享平台，又投入支持引导经费，对供需双方企业给予奖励。由这些举措提炼为“主动服务”。",
        "材料二第二组：网红县长面对疫情导致茶叶销售困难，巧借直播和热点，学习表达传播技巧，分析直播数据和流量，组织黑茶网上开园节，帮助农民脱贫攻坚。这里不是主动服务，因为材料重心落在“为百姓办实事、为农民脱贫攻坚”，所以理念应提炼为“为民服务”。",
        "材料三：基层工作细碎难，社会新变化、新现象、新需要层出不穷。第一段采用信息化河长制公示牌，用二维码替代传统公示牌；第二段推行点位长领导责任制，围绕重点区域开展挥发性有机物治理、专项整治和扬尘管控。材料反复出现“细微处”“绣花功夫”“精细化管控”，服务理念就是“精细服务”。",
        "归并方法：三个材料实际有五个故事、五个变局、五个服务理念。材料一拆成柔性服务和创新服务，材料二拆成主动服务和为民服务，材料三合并为精细服务。不要按材料三点作答，也不要把“精准服务”从平台功能里单独提炼出来。",
        "答案组织：总领句必须先写总理念：“服务民生应准确识变、科学应变、主动求变。”后面再按“柔性服务、创新服务、主动服务、为民服务、精细服务”展开，每个分句只保留最能体现理念的新举措，避免把成效铺得过多导致超字数。",
        "易错提醒：第一，不要把服务理念写成服务成效，本题的“其”指新举措；第二，不要漏掉材料一开头的总理念，否则答案只有分类没有统领；第三，不要把居民议事厅带来的圆梦效果当作主要采点；第四，材料三的节约经费、提高效率、幸福感等可理解但不是本题主干，主干是信息化公示牌和点位长责任制体现的精细服务。"
    ],
    "S1-Q19": [
        "课堂起点：老师先把材料里的新职业点清楚：材料一是外卖骑手，材料二是网络监督师、网上导购师、社群管理师，材料三是制冰师和助老员，一共六个新职业。考试时按六个职业逐个写也能操作，但更好的答案要把积极作用归并到若干社会发展领域。",
        "审题总开关：题干要求概括“新职业对社会发展起到的积极作用”，不是概括这些职业的工作流程，也不是只写职业新奇之处。每个职业都要抓“对谁有作用、起到什么作用”，既包括对社会、企业、行业、老人等外部对象的作用，也包括对从业者个人成长、收入、就业自由的作用。",
        "筛选规则：人物经历、订单细节、收费价格、平台数据、冬奥背景和服务站日常场景是定位职业的材料；能体现作用的内容才进答案，如获得晋升、传授经验、保障物资和稳定、找回时间、提高效率、理性消费、维持社群活跃、稳定收入、保障赛事、推动冰雪运动、帮助老人、弘扬美德。",
        "材料一：外卖骑手的积极作用不能只写送餐。小宋从骑手晋升为站长、物流经理，说明新职业提供职业成长通道；他培训骑手、举办站长班，说明能够传授管理经验；疫情中承担医院和隔离点送餐任务，说明外卖骑手在基础物资保障、城市发展乃至社会稳定方面发挥作用。由此归到“服务社会”。",
        "材料二第一层：网络监督师对应年轻人找回时间的需求。监督学习、作息、减肥，与客户制定计划、给出备考建议，是服务内容；小叶复习效率提高，年轻人找回生活和学习时间，增加复习信心，是积极作用。它属于满足个体学习生活需求。",
        "材料二第二层：网上导购师产生于理性消费需求。导购师帮助客户以合理价格达成交易，根据需求提出购物建议，本质上是指导自我约束与理性消费；同时“平台+履约人”模式给年轻人提供更大程度的平等和自由。这里既有对消费者的作用，也有对就业方式的作用。",
        "材料二第三层：社群管理师面向线上社交和企业运营需求。它的作用是帮助企业维持社群活跃度、增强用户黏性；对偏内向的年轻人来说，又提供相对稳定收入，能够扬个人所长。三种职业合并为“满足需求”这一类。",
        "材料三第一层：制冰师服务的是体育比赛和冰雪运动，不要只写“制冰”。高质量冰面保障公开赛和冬奥会等大型赛事顺利开展；中方制冰师和订单班学员在合作实践中成长；冰雪运动热度上升、场地设施供给增加，又说明制冰师推动冰雪运动发展。由此归到“体育比赛和运动”。",
        "材料三第二层：助老员的工作包括清扫环境、陪老人聊天、整理房间、陪同就医、按摩双脚等，核心作用是营造舒适环境、帮助老人。材料结尾把敬老、爱老、助老上升到美德和中国梦，因此答案还要写弘扬美德、践行中国梦。它对应“养老服务”。",
        "归并方法：老师提示，如果考场时间紧，按六个职业顺序写也可以；高配写法是按作用领域归成四类：服务社会、满足需求、体育比赛和运动、养老服务。获得晋升、平等自由、稳定收入、扬个人所长、实现成长这些个人作用可以单独成类，但考场上这样重组工作量大，放入对应职业更稳。",
        "答案组织：总领句可从材料首段提炼为“新职业引领新业态”。后文按四类展开，每类先写领域，再列职业及作用。材料二有三个职业，压缩时要用分号或短句分开，避免把网络监督、导购、社群管理混成一个职业。",
        "易错提醒：第一，不要只罗列六个职业名称，要写积极作用；第二，不要被小宋个人故事、直播订单、制冰系统参数等细节带偏；第三，材料二三种职业都围绕需求，但需求对象不同，要分别点明学习生活、消费、企业社群和从业者发展；第四，助老员不是普通志愿服务，要回到新职业对养老服务和社会美德的作用。"
    ],
    "S1-Q20": [
        "课堂起点：老师先明确，这题问的是特色文化资源在不同领域发挥的作用。作用题不能简单等同于成效，也不能只写做法；通常要写“如何发挥作用”和“发挥了什么作用”，但篇幅重点仍放在后面的作用结果。",
        "审题总开关：题干有三个限制：对象是特色文化资源，任务是服务社会发展，组织方式是不同类别资源在不同领域的作用。三则材料分别对应古籍、地方志、红色档案，作用领域分别是科学研究、文化旅游、展览宣传教育。",
        "筛选规则：得名过程、菜品故事、网友留言、展览名称和举办单位多是背景或载体；真正要筛的是文化资源本身和它带来的作用。古籍看物种特征记载和定名影响，地方志看系统记述和文旅引导，红色档案看信念使命家国情怀的展示及存史资政育人、宣传教育作用。",
        "材料一：文化资源不是“白鱀豚”这个物种本身，也不只是《尔雅》书名，而是古籍中关于珍贵物种的记载。它如何发挥作用？古籍记载了该物种的形态、习性等特征。它发挥了什么作用？指明最先发现者是中国古代劳动人民，推动中文和英文标准名称更改，规范定名，消除“白鳍”等名称带来的误解，并使西方学界开始重视我国文献研究成果。领域可归为科学研究。",
        "材料二：文化资源是《中国淮扬菜志》所代表的地方志。它如何发挥作用？全面系统记述淮扬菜的历史与现状，反映发展历程、菜系特点和美食文化。作用结果包括打造内容广博的食谱和关于淮扬菜的权威信史，并成为旅游达人、美食爱好者的攻略指南。网友留言主要证明这种文旅引导作用，不需要逐条搬运。领域是文化旅游。",
        "材料三：文化资源是红色档案，具体包括家书、日记、手迹档案、历史图片等。它如何发挥作用？展现并诠释共产党人的坚定信念、初心使命和家国情怀。作用结果包括引起社会各界强烈反响和高度赞扬，发挥存史、资政、育人作用，做好党的二十大精神宣传工作，并引导新时代青年传承红色基因。领域可写展览领域，也可理解为红色宣传教育领域。",
        "归并方法：每则材料都按同一个链条处理：先确认文化资源类别，再确认作用领域，再写一句“如何作用”，最后写若干“作用结果”。不要把资源名称、展览活动和社会效果混在一起，否则会变成材料复述。",
        "答案组织：总领句可写“特色文化资源赋能社会发展”。后面用“在科学研究领域、在文化旅游领域、在展览领域”展开。每个分句的主语最好保持为文化资源，如“古籍记载……”“地方志记述……”“红色档案诠释……”，这样能扣住题干的“资源发挥作用”。",
        "易错提醒：第一，不要把群众称呼习惯、网友留言、巡展活动本身当成文化资源主干；第二，不要只写成效而漏掉资源如何发挥作用，比如古籍“记载特征”、地方志“系统记述”、红色档案“展现诠释”；第三，材料三不要把主语写成档案馆或巡展活动，题干主语应回到红色档案；第四，领域分类要同层，不要写成“白鱀豚、淮扬菜、家书”三个对象清单。"
    ],
    "S2-Q01": [
        "课堂起点：进入综合分析中的原因分析题。老师先讲两种思路：故事分析法和主体分析法。本题材料只有一个“同心小院”的故事，但信息琐碎，最稳的办法是提取主体，再按主体归纳原因。",
        "审题总开关：划线句的变化是群众从“旁边看”变成“共同干”，设问要求分析促成转变的原因。原因不是单纯说群众思想觉悟提高，而要回到同心小院这一创新惠民工程如何组织、带动、解决问题、提供指导和用制度激励。",
        "筛选规则：村名、户数、人物姓名、污水沟细节、小院课堂剧场的具体活动场景是材料情境；能解释群众为什么愿意参与的机制才是原因，如便于组织服务、党员召集带头、化解矛盾、征求意见、解决难题、部门指导、院规民约、积分评比、荣誉感增强。",
        "主体一是小院本身。材料开头说“同心小院”是创新惠民工程，组建时综合考虑村民聚居程度、亲缘关系和党员分布，按照便于组织、便于服务原则，以传统院落为中心吸纳周边散居农户。由此概括为“组建科学”，这是群众能够被组织起来的基础原因。",
        "主体二是党员。老章由院民推选、村党支部同意，成为小院召集人；后面老章和其他党员又创设公共卫生责任区，牵头负责日常保洁。看到党员带头，老罗和其他院民才纷纷报名加入。因此这一层原因是“党员带头”，包括成立议事会、推选党员召集人、党员牵头公共事务。",
        "主体三是群众。第一次议事会前，几户院民有矛盾，经过反复劝说后解开心结；污水沟问题通过多次议事会广泛汇集意见建议并反映给上级，最终解决群众反映的难题。这里的原因不是简单“群众参与”，而是群众诉求被尊重、矛盾纠纷被化解、实际问题被解决，形成群众认同。",
        "主体四是部门。有关部门指导下开设“小院课堂”宣讲政策，开设“小院剧场”组织文娱活动，制定院规民约，并通过小院议事会共商院内大小事务。部门指导让小院运行有内容、有规则、有协商渠道，是促成共同参与的重要原因。",
        "主体五是制度。为了调动院民积极性，同心小院以农户为单位采取积分制，每月开展积分评比。积分制带来荣誉感增强、积极性提高和明显变化，因此可概括为“制度引领”或“制度激励”。",
        "归并方法：五个主体对应五个原因：小院组建科学、党员带头、群众认同、部门指导、制度引领。老师提醒，原因分析题不必写成复杂的评价结构，只要把原因分析清楚即可；正面材料中，很多原因本身就是做法，适当带出成效即可。",
        "答案组织：总领句先说明主要原因是推出创新惠民工程、建设同心小院。后文按五个原因展开，每点用“原因词+材料支撑”的结构，例如“组建科学，综合考虑……”“党员带头，成立……”“群众认同，征求……”。",
        "易错提醒：第一，不要只复述污水沟事件，事件背后是议事、上报、解决难题的机制；第二，不要把群众从旁观到参与简单归因于积分制，积分只是五个主体之一；第三，不要漏掉部门指导和院规民约，否则小院如何持续运行讲不清；第四，原因分析题虽然要求观点正确，但这里的观点就是总原因和分原因，不需要额外议论。"
    ],
    "S2-Q02": [
        "课堂起点：这题仍是原因分析，但对象是有争议的执法行为。老师先要求对执法行为作基本判断：砍香樟树被罚有法律依据，也严格走了认定、核实、集体讨论、告知听证、处罚决定等程序，所以具有合法性；之所以引发热议，是因为合法之外还存在合理性和社会接受度问题。",
        "审题总开关：题干问“在执法效果上引发热议的原因”，不能简单判断罚得对不对，也不能只替李先生喊冤。总观点应写成：执法部门依法执法、遵守程序，但也有不合理之处。之后再按主体分析原因。",
        "筛选规则：树的来历、移栽过程、修剪技术标准、罚款倍数、网友提问、官方回应、调查结论都要服务于原因分析。凡是说明为什么公众觉得意外、为什么居民不懂或误解、为什么部门工作不到位、为什么执法解释不足的内容，才是采点。",
        "案件本身原因：第一段“没想到”和罚款十多万元是热搜导火索，说明行政处罚金额巨大且出人意料。最后一段又说近年来居民自发处置树木导致的行政处罚案件激增，说明这不是孤立个案，而是同类矛盾集中显现。",
        "居民和网友原因：李先生认为自己只是修剪树木，不知道过度修剪在法律上会被认定为违规砍伐；网友也不清楚如何合法合规申请修剪。官方回应又说明，香樟树移栽到业主共有绿地后，所有权受到限制，不能擅自处置。由此概括为居民、网友缺乏相关法律知识，并对修剪砍伐认定和树木所有权限制存在不了解、误解。",
        "有关部门原因：李先生修剪前咨询区绿化部门，得到的回应是“不受理”，但审批部门没有明确告知是否需要审批、该如何办理。网友又追问监管部门为什么不早点发现和阻止。最后一段提到小区绿化布局不合理，居民多次呼吁修剪、迁移、砍伐却长时间得不到回应。这些合并为审批回应不明确、监管未及时发现阻止、相关部门不作为，导致居民自己解决又不合规，并承担严重责任。",
        "执法部门原因：执法人员是在事后向李先生解释技术规范和法律认定，说明平时普法宣传不到位；网友质疑之后才由负责人在官方微博回应，也说明案件发生后未能主动以案释法。公众不知道如何合法合规申请，正是普法和解释不足造成的执法效果问题。",
        "归并方法：不要按材料段落平铺，否则容易漏主体。老师最后归成四类主体：有关部门、执法部门、居民网友、案件本身。先用“合法但不够合理”统领，再把四类原因放进去，逻辑会比逐段摘抄清楚。",
        "答案组织：开头先写根本原因，再分点展开。推荐顺序是有关部门原因、执法部门原因、居民网友原因、案件本身原因；如果按课堂分析先写案件本身也可以，但要保证四类主体齐全。",
        "易错提醒：第一，不要把答案写成“执法违法”或“李先生无责”，材料明确有法律依据和程序依据；第二，不要只写罚款金额大，金额只是热议原因之一；第三，不要把“不懂法”全部归咎于居民，执法部门普法不到位也要写；第四，审批部门、监管部门、相关部门都属于有关部门责任，可合并但不能漏。"
    ],
    "S2-Q08": [
        "课堂起点：这是语句理解题，不是让学生介绍“百姓系列”社区工作法本身。老师特别提醒，句子里的重点不是把“服务对象”和“依靠力量”平均拆成两点，而是结合全文理解“依靠的力量”。",
        "审题总开关：这句话出现在材料结尾，和全文倾向一致，所以应先表态“正确、客观、符合实际”。从句意和材料看，重点在后半句“也是我们依靠的力量”，本质含义是要引导居民主动参与社区治理。",
        "筛选规则：语句理解题先找本质含义，再分析。若一句话强调措施做法，课堂给出的路径是“往前看背景问题、往中看措施表现、往后看效果意义”。本题材料从头到尾都围绕居民参与治理，不要硬找大量“服务对象”的材料。",
        "往前看问题：水秀社区是30多年老社区，区域面积大，居民多，新老居民混杂、人员流动频繁、生活方式多样，环境、空间等方面难以满足居民多元化服务需求。第四段的停车、违停、环境和商户生意问题，也可归入这一类社区环境空间与需求不匹配的问题。",
        "往中看做法总括：针对这些问题，水秀社区通过“百姓提案师、百姓规划师、百姓营造师”等社区工作法，引导居民从被动接受管理转向主动参与治理。题干提到工作法，是因为它和这句话的理解密切相关，但作答重点仍是居民参与治理。",
        "百姓提案师：居民提出环境改造要求和第一件提案，第一段也说明微观景观的改造建议、改造创意都来自居民。概括时可写鼓励居民提出暖心实用的改造建议和创意，建立百姓提案常态化机制，并对居民提案积极回应。",
        "百姓规划师：针对稻香路人多车多、违停影响环境和生意的问题，社区干部现场调研、广泛听取居民建议，联合商户设立便民箱，征求100多条规划改造意见，采纳合理建议，并激发商户和居民共同参与社区治理的热情。",
        "往后看效果：居民参与带来了微改造、新面貌，小花园、楼道、角落、座椅等发生暖心实用变化；社区难题得到解决，硬件设施提升，生活环境改变，居民参与治理的积极性大大提高。",
        "答案组织：开头一句表态并解释本质含义；中间按“问题—做法—效果”展开；结尾可顺势提出要重视社区治理，规范、完善、推广百姓系列社区工作法，满足居民多元服务需求。对策可以写，但不能喧宾夺主。",
        "易错提醒：第一，不要把答案分成“居民是服务对象”和“居民是依靠对象”两大块，材料重心不在前者；第二，不要只写百姓提案师、规划师的流程，必须回扣引导居民主动参与治理；第三，第四段的交通停车问题不必单独扩展成大问题，归入环境空间难以满足需求即可；第四，对策是收束，不是主体。"
    ],
    "S2-Q09": [
        "课堂起点：这题和第八题一样属于语句理解或观点论证题。老师先提醒，“不仅……更……”是递进关系，命题重点在后半句，也就是知识是否被传播以及传播的深度和广度，不能平均解释“知识本身价值”和“传播价值”。",
        "审题总开关：结合材料看，这里的“知识”具体是科学知识，“传播”具体是科普知识的传播。因此本质含义可概括为：发挥科学知识的力量，要重视科普工作。先表态这句话正确、客观、符合实际，再围绕科普展开分析。",
        "筛选规则：直播消费金额、平台粉丝量、道具趣味、比赛场景等是材料表现；真正进入答案的是科普为什么必要、怎样科普、科普带来什么效果。仍按课堂的“往前看背景问题、往中看做法表现、往后看效果意义”处理。",
        "往前看必要性：自媒体时代传播以前所未有的速度影响甚至左右生活，这是总背景。保健品直播带货兴起，短视频借“科普养生”名义精准触达并利用老年人健康需求，进行宣传欺诈；网上伪科普误导公众，给患者和群众造成困扰，并占据一定市场。材料六又说科普教育关乎国家未来和民族希望，这说明真科普具有必要性。",
        "往中看方式一：罗医生触网做健康科普，是因为门诊中患者常拿网上问来的答案看病，伪科普会误病情。对应做法是专家主动触网解答群众疑问，让真科普压缩伪科普市场。",
        "往中看方式二：刘医生用情景剧、顺口溜等形式讲术前术后注意事项，本质是把医学健康知识以通俗易懂的方式传递给公众，尤其是老年人。概括时不要停留在“干饭姿势”这个表层例子上。",
        "往中看方式三：吴姥姥作为科普达人，随身携带实验工具，用毛球拍、纸篓、竹扫帚、锅等生活化道具讲物理，并探索短视频科普。这一层可概括为科普达人利用实验工具，探索以短视频形式进行科普。",
        "往后看效果：顺口溜科普打动观众、获得好评；吴姥姥的“不刷题”理念被更多人接受，引导人们思考；科学探索激发好奇心，提升探索欲和探索能力，让公众体会科学的乐趣。这些共同说明传播能放大知识力量。",
        "答案组织：开头写表态和本质含义，中间分“必要性、方式、效果”三层，结尾提出对策：把科普教育作为长远事业发展，丰富科普内容，创新科普形式。对策来自材料中的科普内容和情景剧、短视频等形式，但不要压过分析主体。",
        "易错提醒：第一，不要把答案写成抽象议论文，必须回到科学知识和科普工作；第二，不要只批判直播带货，伪科普只是说明真科普必要；第三，不要把粉丝量、点赞量当作主要效果，真正效果是理念被接受、引导思考、提升探索能力；第四，创新形式和丰富内容是结尾对策，不是材料分析的全部。"
    ],
    "S2-Q10": [
        "课堂起点：老师先判断句式。“坐在办公室碰到的都是问题，深入基层看到的全是办法”不是简单并列，而是通过对比突出后半句，强调深入基层。作答不能平均写“办公室有什么问题、基层有什么办法”，要围绕深入基层展开。",
        "审题总开关：这句话正确、客观、符合实际。本质含义是深入基层才能真正了解群众诉求、解决基层难题。既然强调的是一种措施做法，分析时仍按“往前看基层问题、往中看深入基层的表现、往后看意义效果”组织。",
        "筛选规则：孙金娣任职经历、街道规模、网格数量、群众质疑等是故事材料；要筛的是基层为什么需要深入、干部如何深入、深入后解决了什么。干部个人被报道、被提拔之类不是答案内容。",
        "往前看问题：基层首先有信任问题，群众就在街道对面却从未找街道反映，街道干部也不知情，说明群众不相信政府、不反映问题。其次，仙林片区快速城镇化，发展快、问题多、矛盾复杂，街道财力有限、人手不够、管理力不从心。再次，仙林新村地下空间私占、乱堆杂物、私拉电线，事故纠纷不断。",
        "往中看做法一：仙林街道找准切入点，建立网格化管理，逐步形成社区和网格体系，整合服务办公室，干部下到网格，实现人到格中去、事在网中办、难在网中解、情在网中结。同时建立志愿监督队伍，并通过每日点评、每周小结、每月考评完善考核机制。",
        "往中看做法二：面对干部疑虑和难题，街道干部下沉一线，从班子成员做起，一级做给一级看，一级带着一级干，发挥带头作用，并在实践中总结“网格思想统一法”“同网格一线工作法”等实战技巧。",
        "往中看做法三：针对仙林新村地下空间问题，街道开展整治，把班子成员、社区党员干部、居民志愿者分成工作组，苦口婆心做工作，最终清空杂物、清退租户、排除隐患、重新分配。这里体现的是针对具体问题“一把钥匙开一把锁”，加强精细化治理。",
        "往后看效果：网格化运行后，仙林街道城市管理考核从全市二十多名跃升至前列并连续多年第一；事情越办越少、效率越转越高，很多难题解决在平时，工作负担减轻；干部熟悉社情民意；群众从质疑转为认可，街道赢得群众信任，基层变成充满活力和友爱的大家庭。",
        "答案组织：先表态并解释本质含义，再分三层写问题、办法、效果，最后提出对策：依靠深入基层这一法宝，找准切入点、打破困局，提升基层干部能力素质，并注重长效治理。",
        "易错提醒：第一，不要只写“基层有办法”这种空话，要写出网格化、一线下沉、整治和精细治理；第二，背景问题必须是基层面临的问题，不要写成干部队伍自身问题为主；第三，不要把网格数量、社区数量铺太细，概括为网格化管理即可；第四，对策是从成功经验中提炼，不能替代前面的分析。"
    ],
    "S2-Q11": [
        "课堂起点：这是比较分析题。老师先讲，比较题不能用“材料三讲什么、材料四讲什么”的平铺方式，否则比较感弱；要先找相同点，再按同一维度比较不同点，如本质、方式、目的动机、效果后果。",
        "审题总开关：题干说两种“绣花功夫”，说明两者都有基层工作细碎难这一共同背景，但一个是好的绣花功夫，一个是变味的绣花功夫。作答要明确评价取向：学习前者，避免后者。",
        "筛选规则：二维码、点位长、吃肉问卷、重复签约、迎检等都是案例表现；要筛出可比较的抽象维度。材料三抓信息化治理、精细化管控、治理创新、积极效果；材料四抓应付检查、争相表现、不顾实际、形式主义和消极后果。",
        "相同点：两者面对的都是基层工作细、碎、难，都被材料称为“绣花功夫”。这就是比较的共同背景，不宜写太长，但必须先点出来。",
        "不同点一是本质。材料三的河长制公示牌升级和点位长责任制，属于细微处的治理创新，体现城市治理能力现代化；材料四的问卷答案训练、重复签约、迎检安排，本质是实招不多、虚活不少的形式主义。",
        "不同点二是方式。前者采用信息化河长制公示牌，推行点位长领导责任制，概括为信息化治理和精细化管控；后者不顾实际，做自以为领导满意却让群众失望的事，如为检查过关教村民答话、反复组织签约、把企业变成迎检专业户。",
        "不同点三是目的动机。材料三开头说社会新变化、新现象、新需要层出不穷，前者是为了应对这些基层治理新情况；材料四则是为了应付上级检查考核、争相表现所谓发展成果。老师用“应对”和“应付”区分两者。",
        "不同点四是效果后果。前者节约维护经费、提高工作效率，增加新的服务功能，方便群众反映问题，并提升群众蓝天幸福感；后者给企业造成困扰，耗费基层干部精力，浪费财政资金，基层工作的聚焦点被带偏。",
        "答案组织：结构可写为“相同点+不同点+结论对策”。不同点最好按本质、方式、目的、效果四个维度逐条比较，每条都用“前者……后者……”的句式，阅卷定位更清楚。",
        "易错提醒：第一，不要只写两个案例简介，要真正比较；第二，材料三的二维码和点位长不要拆得过细，概括成信息化治理、精细化管控；第三，材料四不要只批评基层干部，要抓其形式主义本质和应付检查目的；第四，结尾对策要正反结合：持之以恒、变中出新，找准聚焦点，做好真正的绣花功夫。"
    ],
    "S2-Q12": [
        "课堂起点：这题继续考比较分析，但比较对象很明确，是“不见面办公”和“不见面治理”。老师提醒，不能简单评价线上化好或坏，而要在同一背景下比较两种工作方式的行为、效果和社会评价。",
        "审题总开关：两者相同点是信息化时代新技术、新手段应用成为常态。不同在于，前者是服务窗口因疫情转为线上后仍能解决群众急难的正面案例；后者是用上网代替下基层，导致治理脱离实际的反面或需改进案例。",
        "筛选规则：王女士女儿高考、档案无法寄送、村群名单、下水管网等都是具体情境；要筛出比较维度。前者抓居家办公、线上受理、绿色通道、容缺受理；后者抓干部不见群众、微信电话处理、通知不到位、不符合实际、问题未解决。",
        "相同点：两个案例都发生在信息化时代，都使用了新技术新手段办理事务或治理事务。这个共同背景应简洁写出，不要把它写成结论。",
        "不同点一是方式。不见面办公是窗口全面转为居家办公，通过电子邮件、QQ业务群等线上服务方式受理业务，并依据申办人员实际情况开通绿色通道、实行容缺受理；不见面治理则是干部不与群众见面，把上网代替下基层，仅靠微信群通知、电话问问来安排和处理事情。",
        "不同点二是效果。不见面办公解决了服务对象无法到现场提交个人申请的困局；不见面治理导致通知不到位、名单不符合实际、效率并不高，反而给群众带来不便，也没有解决群众长期反映的现实问题。",
        "不同点三是社会评价。不见面办公受到群众一致称赞；不见面治理中干部认为提高效率、便捷群众，但群众并不认同，说明干部和群众想法不一致，社会评价存在落差。",
        "归并方法：前者可学习，后者要改进，但后者不是完全不能用信息化手段。关键是“上网不能代替下基层”，线上工具只能辅助治理，不能替代干部了解群众、解决问题。",
        "答案组织：按“相同点—不同点—结论对策”写。不同点用方式、效果、社会评价三个维度，保持每个维度都同时比较前者和后者。结尾写学习前者、改进后者，创新服务方式，加强部门监督，线上线下相结合。",
        "易错提醒：第一，不要把不见面办公和不见面治理都简单归为信息化创新；第二，不要只写疫情背景，背景不是主要得分点；第三，不见面治理的核心问题不是微信群本身，而是脱离实际、不到基层；第四，结尾不要全盘否定线上治理，应强调改进和结合。"
    ],
    "S2-Q13": [
        "课堂起点：老师把这题归为双观点现象分析题。题干要求评析网友的不同观点，不是简单评价粉丝文化，也不是在两种观点中选边站。",
        "审题总开关：第一步要先概括两种观点并表态。反对方认为粉丝文化不值得提倡，支持方认为粉丝文化推动社会进步；两种观点都有依据，但都只看到一面，所以均偏颇、片面，应全面看待。",
        "筛选规则：材料中的长句很多是论据，不是观点本身。比如“大量占用公共资源和社会注意力”是反对观点的论据，“隐秘问题被披露”是支持观点的论据。作答时先提炼观点，再用正反分析回应观点。",
        "正面分析：粉丝力量的崛起是无法回避的事实。粉丝文化可以为粉丝提供信仰和前进动力，为偶像赢得粉丝和收益；在Z演员事件中，网友搜索论文、导师和母校，揭露明星学术不端问题，使知网学术资源垄断问题进入公众视野，也颠覆了人们对全媒体时代的传统认知。",
        "反面分析：粉丝文化也是复杂现象，存在明显问题。咪蒙案例引发论战、招致骂名和公众不满，助长为了吸粉而发布虚假内容、传播不良文化、贩卖焦虑、骗取流量；粉丝之间为偶像争执，会造成人际关系不和；明星相关内容大量占用新闻资源，也容易挤占社会注意力。",
        "对策一看偶像和博主。材料里偶像、博主为了吸粉挑起对立、发布虚假内容、传播不良文化，所以应加强自律，管好自身表达和内容生产，传播正能量。",
        "对策二看粉丝。材料反复出现“粉丝力量”，但狂刷流量、争吵护偶像不是理性方式。粉丝要理性宣示力量，同时通过举报、披露问题等方式发挥监督作用。",
        "对策三看政府和平台。材料中有用户举报、平台监测、政策法规和平台规定，据此可提出政府和平台要加强政策法规宣传，强化监管，打击发布虚假内容、传播不良文化等违规行为。",
        "答案组织：结构是“概括观点并表态—正面分析—反面分析—主体对策”。正反分析现象本身，就已经在反驳两种片面观点，不必逐句拆解每个网友观点。",
        "易错提醒：第一，不要把观点原文长句整段照抄，要提炼成支持与反对两种观点；第二，不要只写粉丝文化不好，材料也有揭露学术不端、推动问题进入公众视野等正面内容；第三，不要把所有责任推给粉丝，偶像、平台、政府都有对应责任；第四，正反分析要围绕粉丝文化，不要泛化成自媒体治理大作文。"
    ],
    "S2-Q18": [
        "课堂起点：这是公共行为评析题。老师先讲，评价执法行为不能只说好或坏，要用辩证思维，先总体定性，再正面评价、反面评价，最后提出对策。定性时常从合法性和合理性两个角度入手。",
        "审题总开关：本题总体评价是：执法部门允许农民在火车站广场晒谷的处理具有一定合理性，但合法性和规范性方面存在不足。不能全盘肯定，也不能全盘否定。",
        "筛选规则：网友晒图、火车站广场成“金色海洋”、农民晒谷场景是引入；要筛的是执法部门处理的正面依据、反面问题和后续改进方向。正面点看需求、论证、管理措施；反面点看规范、通行安全、形象、沟通程序和知情权。",
        "正面评价：执法部门一开始指出农民在广场晒谷行为不妥，说明并非完全放任；同时考虑周边农民秋收时节缺少晾晒场地的现实需求和实际情况，经过充分论证，采取管理措施，通过引导给农民方便，并区分农忙时节和非农忙时节，避免简单一刀切。",
        "正面措施还体现在具体管理要求上：留出旅客进出站通道和必要活动空间，保证不影响通行；除稻谷外不允许堆放其他物品，保证整齐美观；晚上6点前清理干净，保证场地卫生。这些说明其处理具有一定合理性。",
        "反面评价：火车站广场本应安检严格、管理规范，允许晒谷违反广场管理规范，损害车站整洁形象；即使一般情况下不影响通行和安全，特殊情况下仍可能影响旅客出行，存在公共安全隐患；网友质疑、旅客可能心理抵触，说明社会接受度不足。",
        "反面程序问题更关键：执法部门事先没有与火车站沟通协调，车站方面不知情，车站管理人员因此常与农民发生争执。这说明程序和部门协同不足，也侵犯了相关管理方的知情权，是合法性方面存在不足的重要依据。",
        "答案组织：开头先定性“合理但合法性不足”，中间分正面和反面两组写，最后提出对策。不要把材料顺序照搬成流水账，正面放一起，反面放一起，评价才清楚。",
        "对策生成：针对违反规范，提出依法执法、规范管理；针对未沟通协调，提出做好部门协调，并在农民和旅客之间寻找平衡点；针对网友质疑和旅客心理抵触，提出加强宣传引导，争取社会理解与支持。",
        "易错提醒：第一，不要把允许晒谷写成完全错误，材料明确有现实需求和管理措施；第二，不要只写农民缺场地，也要写广场规范和公共安全隐患；第三，不要漏掉未与车站沟通这一程序问题；第四，结尾对策要按执法部门、车站协调、旅客网友理解三个方向展开。"
    ],
}


MANUAL_SELECTED_IDS: dict[str, list[str]] = {
    "R20240626-173024": ["S5-Q02", "S7-Q08"],
    "R20240626-183025": ["S5-Q02", "S6-Q03"],
    "R20240627-063353": ["S2-Q02", "S2-Q08"],
    "R20240627-073354": ["S2-Q08", "S2-Q09"],
    "R20240627-123341": ["S2-Q12", "S2-Q13"],
    "R20240627-163207": ["S5-Q08", "S6-Q04"],
    "R20240627-193210": ["S6-Q06", "S7-Q10", "S5-Q09"],
    "R20251004-081004": ["S6-Q06", "S5-Q09", "S7-Q10"],
    "R20251004-101006": ["S5-Q07", "S7-Q03", "S6-Q07"],
}


MANUAL_UNCONFIRMED: dict[str, str] = {
    "R20240625-001117": "转写过短且主要是闲聊/收尾，缺少可提炼的有效讲解。",
    "R20240626-193026": "转写过短且夹杂课间闲聊，相关 S7-Q08 讲解已由 R20240626-173024 覆盖。",
    "R20240627-153344": "转写仅为过渡句，无法形成题目讲解。",
    "R20251005-225915": "课后问答跨多个题目，话题频繁切换，无法可靠切分为单题讲解。",
    "R20251006-222617": "课后问答跨应用文、大作文和多道执法主题题，无法可靠切分为单题讲解。",
}


MANUAL_BULLETS: dict[tuple[str, str], list[str]] = {
    ("R20240626-173024", "S5-Q02"): [
        "审题定位：宣讲稿的核心目的是吸引大学毕业生到乡村干事创业，不能写成一般乡村振兴材料摘抄。",
        "材料拆解：刘胜、小丽、小许分别对应干事立业、投资创业、发展事业，可用三个“业”形成清晰分类。",
        "采点思路：乡村优势可概括为发展事业的第一站、干事立业的大舞台、投资创业的新天地。",
        "答案组织：先写乡村振兴背景和宣讲目的，再分写政策前景、机遇未来、环境乡愁，最后发出报名号召。",
        "易错提醒：不要逐段照抄材料，应围绕“农村有什么优势、对大学生有什么好处”重组材料顺序。",
    ],
    ("R20240626-173024", "S7-Q08"): [
        "审题定位：主题是行政执法与良性互动，重点不是泛谈法治，而是说明执法者和被执法者如何相向而行。",
        "材料拆解：良性互动可解释为从管理到服务、从对立到并行、从纸上法治到行动法治。",
        "采点思路：从“怎么办”破题，围绕精细化执法、柔性化执法、规范化执法组织分论点。",
        "答案组织：分论点可写创新执法方式、凝聚执法力量、提升执法效能，再落到开创善管善治新局面。",
        "易错提醒：落脚点要回到良法善治、社会共治和群众法治获得感，避免只写执法技术或一般治理口号。",
    ],
    ("R20240626-183025", "S5-Q02"): [
        "审题定位：宣讲稿要服务“吸引大学毕业生来乡村干事创业”这一目的，结构上应有背景、主体理由和呼吁号召。",
        "材料拆解：主体理由可按有政策有前景、有机遇有未来、有环境有乡愁三层展开。",
        "采点思路：小许对应政策待遇和发展前景，刘胜对应干事立业和晋升机遇，小丽对应产业创业和技术资金支持。",
        "答案组织：材料顺序可以重组，不必照抄；先交代乡村振兴和人才需求，再穿插三个人物事例支撑三个分标题。",
        "易错提醒：结尾要回到“农村机会吸引人、农村环境留住人、来乡村大显身手”的号召，避免只写材料复述。",
    ],
    ("R20240626-183025", "S6-Q03"): [
        "审题定位：文章主题是文化创新赋能新质生产力、文化创造助力奋进新征程，分类点要前后对应。",
        "材料拆解：开头可用文化创新创造、文旅新赛道、群众智慧和力量等材料金句组合成排比。",
        "采点思路：可从跨界融合、科技赋能、文化企业创新创造等角度提炼分论点。",
        "答案组织：论证时用苏州博物馆文创跨界融合、江苏文化企业数字技术赋能等材料作例证。",
        "易错提醒：分论点字数和句式要尽量一致，不要只堆素材而缺少“新质生产力”的主题牵引。",
    ],
    ("R20240627-063353", "S2-Q02"): [
        "审题定位：这是原因分析题，核心评价“砍香樟树被罚”为何引发热议，不能只判断处罚对错。",
        "材料拆解：老师将争议归结为“合法但不够合理”，先说明执法有法律依据和程序依据，再分析不合理之处。",
        "采点思路：按主体分类更稳，包括有关部门、执法部门、居民网友和案件本身四类原因。",
        "答案组织：有关部门回应不清、监管不早、相关部门不作为；执法部门平时普法和事后以案释法不足；居民网友不了解或误解法律。",
        "易错提醒：不要从材料第一段一路平铺，容易漏主体；先拎主体再回材料找证据，能减少漏点。",
    ],
    ("R20240627-063353", "S2-Q08"): [
        "审题定位：这题考语句理解，“每位居民也是依靠的力量”是递进重点，不能把服务对象和依靠对象平均拆分。",
        "材料拆解：全文都围绕居民参与治理展开，依靠力量的本质是引导居民主动参与社区治理。",
        "采点思路：分析顺序是往前看背景问题、往中看措施表现、往后看治理效果。",
        "答案组织：背景写环境与空间难以满足居民多元需求；做法写百姓提案师、规划师、营造师等社区工作法。",
        "易错提醒：对策不是重点，但可以在结尾补完善、规范、推广社区工作法等建议。",
    ],
    ("R20240627-073354", "S2-Q08"): [
        "审题定位：续讲重点仍是社区工作法的理解，答案主体应突出居民参与治理。",
        "材料拆解：百姓提案师强调居民提建议、建立常态机制、积极回应；百姓规划师强调征求建议、采纳合理建议、激发参与热情。",
        "采点思路：效果可写解决社区难题、改善硬件设施、改变生活环境、提高居民参与积极性。",
        "答案组织：在分析后补少量对策，如完善规范推广百姓系列社区工作法、匹配居民多元服务需求。",
        "易错提醒：综合分析题中对策不能喧宾夺主，分析才是主要得分层。",
    ],
    ("R20240627-073354", "S2-Q09"): [
        "审题定位：“知识的力量”重点在传播，尤其是科普知识传播的深度和广度。",
        "材料拆解：先分析背景问题，如伪科普、直播带货欺诈、误导公众；再分析真科普的表现。",
        "采点思路：科普做法包括专家主动触网答疑、用通俗易懂方式传递科学知识、科普达人用实验工具和短视频科普。",
        "答案组织：意义写打动观众、获得好评、让科学理念被更多人接受、激发好奇心和探索能力。",
        "易错提醒：不要只解释“知识本身价值”，要回到传播价值，并在结尾提出丰富内容、创新形式、长期发展科普教育。",
    ],
    ("R20240627-123341", "S2-Q12"): [
        "审题定位：比较“不见面办公”和“不见面治理”，不能只评价线上化好坏。",
        "材料拆解：两者共同背景是信息化时代新技术新手段应用成为常态。",
        "采点思路：比较角度可抓行为方式、实际效果和社会评价；前者正面，后者需要改进。",
        "答案组织：不见面办公写线上受理、绿色通道、容缺受理，效果是解困并获称赞；不见面治理写上网代替下基层，导致通知不到位、不符合实际、问题未解决。",
        "易错提醒：后者不是完全否定，而是要线上线下结合、深入基层、加强监督。",
    ],
    ("R20240627-123341", "S2-Q13"): [
        "审题定位：粉丝文化是双观点现象分析题，要评析不同观点，而不是简单站队。",
        "材料拆解：先概括支持和反对两种观点并表态，两种观点都只看到一面，因而片面。",
        "采点思路：正面分析粉丝文化提供信仰动力、揭露问题、推动社会关注；反面分析占用新闻资源、发布虚假内容、传播不良文化等问题。",
        "答案组织：最后按主体提对策，偶像要自律并传播正能量，粉丝要理性表达并发挥监督作用，政府和平台要宣传规则并监管打击违规行为。",
        "易错提醒：材料中的长句很多是论据，不是观点本身；先提炼观点再分析现象。",
    ],
    ("R20240627-163207", "S5-Q08"): [
        "审题定位：这题是宣传戴窑稻米产业特色，不是单纯卖产品，要写产业发展经验。",
        "材料拆解：总做法是注重品牌建设，探索优质大米种植、加工、销售全产业链发展之路。",
        "采点思路：可按科技创新、设施完善、标准制定、网络销售、文化传承等特色分层。",
        "答案组织：主体适合用“突出某字+与某方面结合+推动特色产业某成效”的并列结构。",
        "易错提醒：应用文主体要偏务实，围绕产业特色和成功经验，不要写成空泛议论文。",
    ],
    ("R20240627-163207", "S6-Q04"): [
        "审题定位：主题两句话中，“科普孕育科创”是重点，“科创领航未来”是辅助落脚。",
        "材料拆解：总书记指示中涉及科学家、青少年、全民族三个主体，要从科普对象和科创主体两头理解。",
        "采点思路：推进科普可写投身科普事业、优化科普内容、拓展科普形式。",
        "答案组织：落脚到激发青少年科学兴趣、提高全民族科学素质、推动创新发展和高水平科技自立自强。",
        "易错提醒：不能只写科技创新，应突出科学普及对科技创新的基础性作用。",
    ],
    ("R20240627-193210", "S6-Q06"): [
        "审题定位：这是对策性文章，重点在“新作为”，不是单纯描述新职业、新业态。",
        "材料拆解：新作为可分为加强政府管理力度、拓展政府服务宽度、提升政府保障温度。",
        "采点思路：具体对策分别对应与时俱进、适度引导、关心关爱。",
        "答案组织：三段可写新时代危机并存需加强管理，新业态野蛮生长需拓展服务，新职业面临窘境需提升保障。",
        "易错提醒：“拓展政府服务宽度”比“拓宽政府服务宽度”更顺，且服务、管理、保障不能混为一谈。",
    ],
    ("R20240627-193210", "S7-Q10"): [
        "审题定位：核心不是泛谈公平正义，而是用“近人、达情、善治”解释如何促进严格规范公正文明执法。",
        "材料拆解：“近人”指深入基层、走进百姓；“达情”指换位思考、通达民情；“善治”指依靠法治追求善治。",
        "采点思路：分论点可围绕深入基层、换位思考、依靠法治三条展开。",
        "答案组织：论证中要结合群众办事、社区治理、虚假保健品监管等材料，最后落到公平正义就在身边。",
        "易错提醒：主题太长时不要硬拆成严格、规范、公正、文明四段，可用三段式承接古语逻辑。",
    ],
    ("R20240627-193210", "S5-Q09"): [
        "审题定位：发言稿主题不是“四个字”培训要点，而是培训后如何发挥驻村第一书记带头人作用。",
        "材料拆解：培训学习内容可归纳为政策学习、产业发展、美丽乡村和乡村协作。",
        "采点思路：三个带头人分别是政策落实的带头人、产业发展的带头人、村民致富的带头人。",
        "答案组织：开头交代培训背景和学习收获，主体分写加强学习提升能力、明确方向突出特色、因地制宜优势互补。",
        "易错提醒：只围绕“找准路子、用好点子”等四个要点会跑偏，应回到“发挥带头人作用”。",
    ],
    ("R20251004-081004", "S6-Q06"): [
        "审题定位：文章要围绕“新时代潮涌新职业，新业态呼唤新作为”，把重点放在政府如何作为。",
        "材料拆解：开头可用从外卖骑手到网络监督师、从云办公到云娱乐等排比引出新职业新业态。",
        "采点思路：三段分别写政府管理与时俱进、政府服务适度引导、政府保障关心关爱。",
        "答案组织：每段先分析问题或背景，再写具体对策，体现对策性文章特征。",
        "易错提醒：新职业、新业态既是落脚点也是问题背景，不要只写成材料现象罗列。",
    ],
    ("R20251004-081004", "S5-Q09"): [
        "审题定位：这是村两委联席会发言稿，主题应提炼为发挥带头人作用。",
        "材料拆解：培训背景、学习收获和回村打算要自然衔接，不能只列培训日程。",
        "采点思路：主体可写做政策落实、产业发展、村民致富三个带头人。",
        "答案组织：每段用材料中的培训内容支撑回村后的行动计划，结尾号召基层干部长见识、换脑子、带群众干。",
        "易错提醒：发言稿要有开场白和呼吁号召，但不要写空泛客套话。",
    ],
    ("R20251004-081004", "S7-Q10"): [
        "审题定位：围绕“善治须达情，达情始近人”写执法文章，先解释古语，再落到执法实践。",
        "材料拆解：近人是执法前提，达情是执法关键，善治是执法目标。",
        "采点思路：可用从被群众骂到怕听不到骂声、从闭门羹到敲开门等对比句写开头。",
        "答案组织：主体围绕深入基层、通达民情、依靠法治展开，材料案例要服务分论点。",
        "易错提醒：不要把古语解释和总书记讲话割裂，要落到严格规范公正文明执法和群众公平正义。",
    ],
    ("R20251004-101006", "S5-Q07"): [
        "审题定位：工作简报围绕“土特”文章和特色产业新动能，核心是如何做。",
        "材料拆解：材料可归纳为转变生产结构、合作研发、优化人才环境等做法。",
        "采点思路：新动能可对应新格局、新品牌、新业态，形成三个分论点。",
        "答案组织：突出“创”字转变结构，围绕“精”字推进合作研发，坚持“引”字优化人才环境。",
        "易错提醒：不要只写产业现状，应写具体工作措施和带来的新动能。",
    ],
    ("R20251004-101006", "S7-Q03"): [
        "审题定位：“用智慧给执法加分，以创新促执法乘效”指向提升行政执法质量和效能。",
        "材料拆解：智慧对应执法质量，创新对应执法效能，落脚仍是行政执法。",
        "采点思路：提升路径可写科技赋能、能力建设、宽严相济。",
        "答案组织：分论点可表述为以智慧执法加分、以创新执法乘效、以为民执法护航。",
        "易错提醒：不要只解释“加分”“乘效”字面含义，要回答提升什么、如何提升、为谁提升。",
    ],
    ("R20251004-101006", "S6-Q07"): [
        "审题定位：主题重点是“网追正能量”，不是泛泛批判网络问题。",
        "材料拆解：网络正能量具有及时到达、无处不在、放大主流价值的特点。",
        "采点思路：推广正能量可从创新治理、坚守责任、服务社会三个角度展开。",
        "答案组织：结合“网络语言即网络生活”，把正能量落到美好生活、真实生活、和谐生活、安全生活。",
        "易错提醒：材料里的网络问题只有在服务正能量主题时才写，不能把文章写成网络乱象治理。",
    ],
}


@dataclass(frozen=True)
class Question:
    id: str
    chapter_index: int
    chapter_title: str
    question_index: int
    title: str
    page: int | None
    start_line: int
    end_line: int
    block: str
    question_text: str
    answer_text: str
    match_text: str
    keywords: list[str]

    def to_json(self) -> dict[str, Any]:
        return {
            "id": self.id,
            "chapter_index": self.chapter_index,
            "chapter_title": self.chapter_title,
            "question_index": self.question_index,
            "title": self.title,
            "page": self.page,
            "start_line": self.start_line,
            "end_line": self.end_line,
            "question_text": self.question_text,
            "answer_text": self.answer_text,
            "match_text": self.match_text,
            "keywords": self.keywords,
        }


def rel(path: Path | None) -> str:
    if path is None:
        return ""
    try:
        return str(path.relative_to(ROOT)).replace("\\", "/")
    except ValueError:
        return str(path).replace("\\", "/")


def read_text(path: Path) -> str:
    return path.read_text(encoding="utf-8-sig")


def write_json(path: Path, data: Any) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(data, ensure_ascii=False, indent=2) + "\n",
        encoding="utf-8",
    )


def read_json(path: Path) -> Any:
    return json.loads(read_text(path))


def chinese_num_to_int(raw: str) -> int | None:
    raw = raw.strip()
    if not raw:
        return None
    if raw.isdigit():
        return int(raw)
    total = 0
    current = 0
    seen = False
    for char in raw:
        if char not in CHINESE_NUMS:
            return None
        value = CHINESE_NUMS[char]
        seen = True
        if value == 100:
            current = max(current, 1) * 100
            total += current
            current = 0
        elif value == 10:
            current = max(current, 1) * 10
            total += current
            current = 0
        else:
            current = value
    if seen:
        total += current
        return total
    return None


def int_to_chinese(num: int) -> str:
    digits = "零一二三四五六七八九"
    if num <= 10:
        return "十" if num == 10 else digits[num]
    if num < 20:
        return "十" + digits[num - 10]
    if num < 100:
        tens, ones = divmod(num, 10)
        return digits[tens] + "十" + (digits[ones] if ones else "")
    return str(num)


def compact_text(text: str, limit: int | None = None) -> str:
    text = TIMESTAMP_RE.sub("", text)
    text = re.sub(r"R\d{8}-\d{6}[^\n]*", "", text)
    text = PUNCT_RE.sub("", text)
    if limit is not None:
        return text[:limit]
    return text


def display_excerpt(text: str, limit: int = 220) -> str:
    cleaned_lines: list[str] = []
    for line in text.splitlines():
        line = line.strip()
        if not line or SPEAKER_RE.match(line):
            continue
        cleaned_lines.append(line)
    cleaned = " ".join(cleaned_lines)
    cleaned = re.sub(r"\s+", " ", cleaned).strip()
    if len(cleaned) <= limit:
        return cleaned
    return cleaned[:limit].rstrip() + "..."


def clean_transcript_text(text: str) -> str:
    lines: list[str] = []
    for line in text.splitlines():
        line = line.strip()
        if not line or SPEAKER_RE.match(line):
            continue
        if R_ID_RE.search(line) and line.upper().endswith(".WAV"):
            continue
        lines.append(line)
    return "\n".join(lines).strip()


def split_sentences(text: str) -> list[str]:
    text = clean_transcript_text(text)
    parts = re.split(r"(?<=[。！？!?])\s*|\n+", text)
    sentences = [re.sub(r"\s+", "", part).strip() for part in parts]
    return [sentence for sentence in sentences if len(sentence) >= 8]


def make_ngrams(text: str, limit: int | None = None) -> set[str]:
    compact = compact_text(text, limit=limit)
    grams: set[str] = set()
    for size in (2, 3, 4):
        if len(compact) <= size:
            continue
        grams.update(compact[i : i + size] for i in range(len(compact) - size + 1))
    return grams


def cosineish_score(a: set[str], b: set[str]) -> float:
    if not a or not b:
        return 0.0
    return len(a & b) / math.sqrt(len(a) * len(b))


def extract_keywords(text: str, *, limit: int = 20) -> list[str]:
    keywords: list[str] = []
    for pattern in (r"“([^”]{2,18})”", r"《([^》]{2,18})》", r"[A-Za-z][A-Za-z0-9+\-.]{1,18}"):
        for match in re.findall(pattern, text):
            if match not in keywords:
                keywords.append(match)

    compact = re.sub(r"[^\u4e00-\u9fffA-Za-z0-9]+", " ", text)
    chunks = [chunk for chunk in compact.split() if 2 <= len(chunk) <= 12]
    counts = Counter(chunks)
    for chunk, _ in counts.most_common(80):
        if any(stop in chunk for stop in STOP_TERMS):
            continue
        if chunk.isdigit():
            continue
        if chunk not in keywords:
            keywords.append(chunk)
        if len(keywords) >= limit:
            break
    return keywords[:limit]


def extract_sections(block: str) -> tuple[str, str]:
    question_marker = "### 📘 题目与材料"
    answer_marker = "### ✍️ 参考答案"
    question_text = ""
    answer_text = ""
    if question_marker in block and answer_marker in block:
        before_answer, answer_part = block.split(answer_marker, 1)
        question_text = before_answer.split(question_marker, 1)[1].strip()
        answer_text = answer_part.strip()
    elif answer_marker in block:
        before_answer, answer_part = block.split(answer_marker, 1)
        question_text = before_answer.strip()
        answer_text = answer_part.strip()
    else:
        question_text = block.strip()
    answer_text = re.sub(r"\n---\s*$", "", answer_text).strip()
    return question_text, answer_text


def parse_questions() -> list[Question]:
    if not SOURCE_MD.exists():
        raise FileNotFoundError(f"Missing source Markdown: {SOURCE_MD}")

    lines = read_text(SOURCE_MD).splitlines()
    chapter_by_line: dict[int, tuple[int, str]] = {}
    chapter_index = 0
    chapter_title = ""
    for idx, line in enumerate(lines):
        match = CHAPTER_HEADING_RE.match(line)
        if match:
            chapter_index += 1
            chapter_title = match.group(1).strip()
        chapter_by_line[idx] = (chapter_index, chapter_title)

    starts = [idx for idx, line in enumerate(lines) if QUESTION_HEADING_RE.match(line)]
    questions: list[Question] = []
    q_counts: Counter[int] = Counter()
    for pos, start in enumerate(starts):
        end = starts[pos + 1] if pos + 1 < len(starts) else len(lines)
        title_match = QUESTION_HEADING_RE.match(lines[start])
        if not title_match:
            continue
        title = title_match.group(1).strip()
        chapter_idx, chapter = chapter_by_line[start]
        q_counts[chapter_idx] += 1
        question_idx = q_counts[chapter_idx]
        block = "\n".join(lines[start:end]).rstrip()
        page_match = META_PAGE_RE.search(block)
        page = int(page_match.group(1)) if page_match else None
        question_text, answer_text = extract_sections(block)
        match_text = "\n".join(
            [
                title,
                question_text[:6000],
                answer_text[:2000],
            ]
        )
        keywords = extract_keywords("\n".join([title, question_text[:1500], answer_text[:1000]]))
        questions.append(
            Question(
                id=f"S{chapter_idx}-Q{question_idx:02d}",
                chapter_index=chapter_idx,
                chapter_title=chapter,
                question_index=question_idx,
                title=title,
                page=page,
                start_line=start + 1,
                end_line=end,
                block=block,
                question_text=question_text,
                answer_text=answer_text,
                match_text=match_text,
                keywords=keywords,
            )
        )
    return questions


def parse_hints(name: str) -> tuple[list[int], list[int], list[str]]:
    page_hints: set[int] = set()
    question_hints: set[int] = set()

    for raw in re.findall(r"P(\d{1,3})", name, flags=re.IGNORECASE):
        page_hints.add(int(raw))
    for raw in re.findall(r"第\s*(\d{1,3})\s*页", name):
        page_hints.add(int(raw))
    for raw in re.findall(r"第\s*([一二三四五六七八九十百两\d]{1,6})\s*题", name):
        value = chinese_num_to_int(raw)
        if value is not None:
            question_hints.add(value)
    for raw in re.findall(r"([一二三四五六七八九十百两]{1,6})、", name):
        value = chinese_num_to_int(raw)
        if value is not None:
            question_hints.add(value)

    cleaned = re.sub(r"\.[A-Za-z0-9]+", " ", name)
    cleaned = R_ID_RE.sub("", cleaned)
    cleaned = re.sub(r"P\d{1,3}", "", cleaned, flags=re.IGNORECASE)
    cleaned = re.sub(r"第[一二三四五六七八九十百两\d]{1,6}[页题]", "", cleaned)
    name_terms = extract_keywords(cleaned, limit=12)
    return sorted(page_hints), sorted(question_hints), name_terms


def scan_transcripts() -> dict[str, Any]:
    audio_by_id: dict[str, Path] = {}
    for path in sorted(AUDIO_DIR.glob("*")):
        if not path.is_file():
            continue
        match = R_ID_RE.search(path.name)
        if match:
            audio_by_id[match.group(1)] = path

    transcript_by_id: dict[str, Path] = {}
    for path in sorted(TRANSCRIPTS_DIR.glob("*.md")):
        match = R_ID_RE.search(path.name)
        if match:
            transcript_by_id[match.group(1)] = path

    records: list[dict[str, Any]] = []
    for r_id, transcript_path in sorted(transcript_by_id.items()):
        audio_path = audio_by_id.get(r_id)
        transcript_text = read_text(transcript_path)
        joined_names = transcript_path.name + " " + (audio_path.name if audio_path else "")
        page_hints, question_hints, name_terms = parse_hints(joined_names)
        records.append(
            {
                "r_id": r_id,
                "transcript_path": rel(transcript_path),
                "audio_path": rel(audio_path),
                "transcript_name": transcript_path.name,
                "audio_name": audio_path.name if audio_path else "",
                "transcript_length": transcript_path.stat().st_size,
                "audio_length": audio_path.stat().st_size if audio_path else None,
                "page_hints": page_hints,
                "question_hints": question_hints,
                "name_terms": name_terms,
                "excerpt": display_excerpt(transcript_text),
                "is_short": transcript_path.stat().st_size < 2000,
            }
        )

    missing_transcripts = []
    for r_id, audio_path in sorted(audio_by_id.items()):
        if r_id not in transcript_by_id:
            page_hints, question_hints, name_terms = parse_hints(audio_path.name)
            missing_transcripts.append(
                {
                    "r_id": r_id,
                    "audio_path": rel(audio_path),
                    "audio_name": audio_path.name,
                    "audio_length": audio_path.stat().st_size,
                    "page_hints": page_hints,
                    "question_hints": question_hints,
                    "name_terms": name_terms,
                }
            )

    return {
        "source": {
            "audio_dir": rel(AUDIO_DIR),
            "transcripts_dir": rel(TRANSCRIPTS_DIR),
        },
        "audio_count": len(audio_by_id),
        "transcript_count": len(transcript_by_id),
        "records": records,
        "audio_without_transcript": missing_transcripts,
    }


def keyword_hits(terms: list[str], text: str) -> list[str]:
    return [term for term in terms if term and term in text]


def build_alignment(questions: list[Question], transcript_data: dict[str, Any]) -> dict[str, Any]:
    q_by_id = {question.id: question for question in questions}
    page_to_questions: dict[int, list[Question]] = defaultdict(list)
    question_no_to_questions: dict[int, list[Question]] = defaultdict(list)
    q_grams = {question.id: make_ngrams(question.match_text, limit=9000) for question in questions}
    q_text_compact = {question.id: compact_text(question.match_text) for question in questions}

    alignments: list[dict[str, Any]] = []
    for record in transcript_data["records"]:
        transcript_path = ROOT / record["transcript_path"]
        transcript_text = read_text(transcript_path)
        transcript_clean = clean_transcript_text(transcript_text)
        transcript_grams = make_ngrams(transcript_clean, limit=16000)

        candidate_scores: dict[str, float] = defaultdict(float)
        candidate_reasons: dict[str, list[str]] = defaultdict(list)

        for page in record["page_hints"]:
            matches = [question for question in questions if question.page == page]
            for question in matches:
                candidate_scores[question.id] += 1.8 if len(matches) == 1 else 1.2
                candidate_reasons[question.id].append(
                    f"filename page hint P{page:03d} matched {len(matches)} question(s)"
                )

        for q_no in record["question_hints"]:
            matches = [question for question in questions if question.question_index == q_no]
            if record["page_hints"]:
                page_set = set(record["page_hints"])
                matches = [question for question in matches if question.page in page_set] or matches
            for question in matches:
                candidate_scores[question.id] += 0.9
                candidate_reasons[question.id].append(f"filename question hint Q{q_no}")

        name_hits_by_q: list[tuple[str, list[str]]] = []
        for question in questions:
            hits = keyword_hits(record["name_terms"], q_text_compact[question.id])
            if hits:
                name_hits_by_q.append((question.id, hits))
        for qid, hits in name_hits_by_q:
            candidate_scores[qid] += min(0.8, 0.18 * len(hits))
            candidate_reasons[qid].append("filename keyword hit: " + "、".join(hits[:5]))

        similarity_rows: list[tuple[str, float]] = []
        for qid, grams in q_grams.items():
            score = cosineish_score(transcript_grams, grams)
            similarity_rows.append((qid, score))
        similarity_rows.sort(key=lambda item: item[1], reverse=True)
        for rank, (qid, score) in enumerate(similarity_rows[:8], start=1):
            if score <= 0:
                continue
            candidate_scores[qid] += score * 2.6
            candidate_reasons[qid].append(f"text similarity rank {rank}: {score:.3f}")

        candidate_rows = []
        for qid, score in candidate_scores.items():
            question = q_by_id[qid]
            candidate_rows.append(
                {
                    "question_id": qid,
                    "score": round(score, 4),
                    "chapter": question.chapter_title,
                    "question_index": question.question_index,
                    "page": question.page,
                    "title": question.title,
                    "reasons": candidate_reasons[qid],
                    "question_keywords": question.keywords[:10],
                }
            )
        candidate_rows.sort(key=lambda row: row["score"], reverse=True)
        candidate_rows = candidate_rows[:8]

        selected_ids = select_questions_for_record(candidate_rows, record)
        confidence, review_required, confidence_reason = classify_alignment(
            candidate_rows,
            selected_ids,
            record,
        )
        alignments.append(
            {
                "r_id": record["r_id"],
                "transcript_path": record["transcript_path"],
                "audio_path": record["audio_path"],
                "transcript_name": record["transcript_name"],
                "audio_name": record["audio_name"],
                "transcript_length": record["transcript_length"],
                "is_short": record["is_short"],
                "page_hints": record["page_hints"],
                "question_hints": record["question_hints"],
                "name_terms": record["name_terms"],
                "selected_question_ids": selected_ids,
                "confidence": confidence,
                "review_required": review_required,
                "confidence_reason": confidence_reason,
                "candidates": candidate_rows,
                "excerpt": display_excerpt(transcript_text, limit=360),
            }
        )

    return {
        "alignments": alignments,
        "confidence_counts": dict(Counter(row["confidence"] for row in alignments)),
    }


def select_questions_for_record(candidate_rows: list[dict[str, Any]], record: dict[str, Any]) -> list[str]:
    if not candidate_rows:
        return []
    top_score = candidate_rows[0]["score"]
    selected: list[str] = []

    # If the filename explicitly names multiple pages, keep every top candidate
    # backed by those page hints.
    if len(record["page_hints"]) > 1:
        page_set = set(record["page_hints"])
        selected = [row["question_id"] for row in candidate_rows if row["page"] in page_set]
    elif record["page_hints"]:
        page_set = set(record["page_hints"])
        page_matches = [row for row in candidate_rows if row["page"] in page_set]
        if page_matches:
            best = max(row["score"] for row in page_matches)
            selected = [
                row["question_id"]
                for row in page_matches
                if row["score"] >= best - 0.5 or row["score"] >= top_score - 0.35
            ]

    if not selected and len(record["question_hints"]) > 1:
        q_set = set(record["question_hints"])
        selected = [row["question_id"] for row in candidate_rows if row["question_index"] in q_set]

    if not selected:
        selected = [candidate_rows[0]["question_id"]]
        for row in candidate_rows[1:4]:
            if row["score"] >= max(1.0, top_score * 0.94):
                selected.append(row["question_id"])

    return list(dict.fromkeys(selected))


def classify_alignment(
    candidate_rows: list[dict[str, Any]],
    selected_ids: list[str],
    record: dict[str, Any],
) -> tuple[str, bool, str]:
    if not candidate_rows or not selected_ids:
        return "low", True, "no candidate"

    top = candidate_rows[0]
    second_score = candidate_rows[1]["score"] if len(candidate_rows) > 1 else 0.0
    has_page = bool(record["page_hints"])
    has_question = bool(record["question_hints"])
    is_cross = len(selected_ids) > 1

    if record["is_short"]:
        return "low", True, "short transcript"

    if has_page and has_question and len(selected_ids) == 1 and top["score"] >= 2.0:
        return "high", False, "page and question hints converge"
    if has_page and len(selected_ids) == 1 and top["score"] >= 2.2 and top["score"] - second_score >= 0.25:
        return "high", False, "unique page-backed candidate"
    if has_page or has_question or is_cross:
        return "medium", True, "filename hint needs review"
    if top["score"] >= 1.05 and top["score"] - second_score >= 0.3:
        return "medium", True, "text similarity lead"
    return "low", True, "weak or ambiguous evidence"


def clean_inline(text: str) -> str:
    return re.sub(r"\s+", " ", text).strip(" ：:；;，,。.\n\t")


def short_clause(text: str, limit: int = 58) -> str:
    text = clean_inline(text)
    text = re.sub(r"^【参考答案】", "", text).strip()
    if len(text) <= limit:
        return text
    cut_positions = [pos for token in "。；;，," if 14 <= (pos := text.find(token)) <= limit]
    if cut_positions:
        return text[: min(cut_positions)].strip(" ：:；;，,。.")
    return text[:limit].rstrip(" ：:；;，,。.") + "等"


def clean_question_title(title: str) -> str:
    title = re.sub(r"^第[一二三四五六七八九十百零两\d]+题[:：]?", "", title)
    title = re.sub(r"（\d+\s*分）", "", title)
    return short_clause(title, limit=74)


def meaningful_prompt(text: str) -> bool:
    compact = compact_text(text)
    return len(compact) >= 8 and "材料" not in compact[:4]


def question_prompt(question: Question) -> str:
    title = clean_question_title(question.title)
    prompt_lines: list[str] = []
    for line in question.question_text.splitlines():
        line = clean_inline(line)
        if not line:
            continue
        if line.startswith("要求") or line.startswith("材料"):
            break
        prompt_lines.append(line)
        if len("".join(prompt_lines)) >= 120:
            break

    prompt = title
    if prompt_lines:
        first = " ".join(prompt_lines)
        if not meaningful_prompt(prompt):
            prompt = first
        elif first.startswith("．．．") or prompt.endswith("请用一段话") or prompt.endswith("请你以"):
            prompt = prompt + first

    prompt = prompt.replace("．．．", "")
    prompt = re.sub(r"（\d+\s*分）", "", prompt)
    prompt = clean_inline(prompt)
    return short_clause(prompt, limit=96) if meaningful_prompt(prompt) else "本题设问"


def clean_reference_answer(answer_text: str) -> str:
    answer = re.sub(r"^【参考答案】", "", answer_text.strip())
    answer = re.sub(r"\n+", " ", answer)
    return clean_inline(answer)


def split_answer_points(answer_text: str) -> tuple[str, list[str]]:
    answer = clean_reference_answer(answer_text)
    if not answer:
        return "", []
    pieces = [piece.strip() for piece in re.split(r"\s*\d+[.．、]\s*", answer) if piece.strip()]
    if len(pieces) <= 1:
        sentences = [sentence.strip() for sentence in re.split(r"[。；;]", answer) if sentence.strip()]
        return "", sentences[:4]
    intro = pieces[0]
    return intro, pieces[1:5]


def chapter_method_hint(chapter_title: str) -> str:
    if "归纳概括" in chapter_title:
        return "这类题重在归纳共性，按题干限定对象提炼要点，不展开议论。"
    if "综合分析" in chapter_title:
        return "这类题要先明确理解或判断，再围绕材料分析原因、表现、影响或对策。"
    if "提出对策" in chapter_title:
        return "这类题要从材料问题反推建议，突出针对性和可操作性。"
    if "应用文" in chapter_title:
        return "这类题要同时照顾身份、对象、文种和行文目的，不能只堆材料点。"
    if "议论文" in chapter_title or "大作文" in chapter_title:
        return "这类题要先稳住立意，再用材料支撑分论点和文章结构。"
    if "行政执法" in chapter_title:
        return "这类题要围绕执法理念、执法方式和群众感受组织材料。"
    return "审题时先锁定作答对象，再决定按原因、做法、效果或对策分层。"


def point_label(point: str) -> str:
    point = clean_inline(point)
    for token in "，,；;。":
        pos = point.find(token)
        if 2 <= pos <= 24:
            return point[:pos].strip(" ：:；;，,。.")
    return short_clause(point, limit=24)


def summarize_points(points: list[str], limit: int = 3) -> str:
    clauses = [point_label(point) for point in points[:limit]]
    return "；".join(clause for clause in clauses if clause)


def transcript_focus_sentence(transcript_text: str, question: Question) -> str:
    cue_terms = {
        "注意",
        "不能",
        "不要",
        "核心",
        "关键",
        "重点",
        "主题",
        "分类",
        "主体",
        "开头",
        "结尾",
        "分论点",
        "答题思路",
    }
    question_terms = set(question.keywords[:12])
    rows: list[tuple[float, int, str]] = []
    for idx, sentence in enumerate(split_sentences(transcript_text)):
        sentence = clean_inline(sentence)
        compact = compact_text(sentence)
        if not 18 <= len(compact) <= 120:
            continue
        if "..." in sentence or any(term in sentence for term in ASR_NOISE_TERMS):
            continue
        score = 0.0
        score += 0.9 * len([term for term in cue_terms if term in sentence])
        score += 0.5 * len([term for term in question_terms if term and term in sentence])
        if "这道题" in sentence or "这篇文章" in sentence:
            score += 0.5
        if score:
            rows.append((score, idx, sentence))
    if not rows:
        return ""
    rows.sort(key=lambda item: (-item[0], item[1]))
    return short_clause(rows[0][2], limit=62)


def extractive_bullets(transcript_text: str, question: Question, *, max_bullets: int = 5) -> list[str]:
    del transcript_text
    intro, points = split_answer_points(question.answer_text)
    title = question_prompt(question)
    point_summary = summarize_points(points)
    bullets: list[str] = [
        f"审题定位：{chapter_method_hint(question.chapter_title)}本题要处理的是：{title}。",
    ]

    if intro:
        bullets.append(f"材料拆解：参考答案的总括判断是“{short_clause(intro)}”，后续要点都应围绕这个判断展开。")
    elif points:
        bullets.append(f"材料拆解：参考答案把材料拆成“{point_summary}”等层次，可据此回到材料定位采分点。")

    if points:
        bullets.append(f"采点思路：可按“{point_summary}”的顺序采点，先抓关键词，再补充材料中的原因、做法或效果。")
    else:
        keywords = "、".join(question.keywords[:4])
        bullets.append(f"采点思路：围绕“{keywords}”等题干关键词筛选材料，避免把无关背景写进答案。")

    if len(points) >= 3:
        bullets.append("答案组织：采用“总括句+分点展开”的结构，每个分点尽量保持同一颗粒度，方便阅卷定位。")
    else:
        bullets.append("答案组织：先回应设问，再用材料依据展开说明，必要时在结尾回到题干要求。")

    bullets.append(f"易错提醒：不要脱离“{title}”泛泛作答，也不要把材料现象原样堆叠成答案。")

    return bullets[:max_bullets]


def make_review_summaries(
    questions: list[Question],
    transcript_data: dict[str, Any],
    alignment_data: dict[str, Any],
) -> dict[str, Any]:
    q_by_id = {question.id: question for question in questions}
    entries: list[dict[str, Any]] = []
    for alignment in alignment_data["alignments"]:
        transcript_text = read_text(ROOT / alignment["transcript_path"])
        question_notes: list[dict[str, Any]] = []
        for qid in alignment["selected_question_ids"]:
            question = q_by_id.get(qid)
            if not question:
                continue
            question_notes.append(
                {
                    "question_id": qid,
                    "status": "needs_review" if alignment["review_required"] else "confirmed",
                    "summary_bullets": extractive_bullets(transcript_text, question),
                    "review_notes": "",
                }
            )
        entries.append(
            {
                "r_id": alignment["r_id"],
                "transcript_path": alignment["transcript_path"],
                "audio_path": alignment["audio_path"],
                "confidence": alignment["confidence"],
                "review_required": alignment["review_required"],
                "status": "needs_review" if alignment["review_required"] else "confirmed",
                "selected_question_ids": alignment["selected_question_ids"],
                "question_notes": question_notes,
                "unconfirmed_reason": "" if not alignment["review_required"] else alignment["confidence_reason"],
            }
        )

    return {
        "schema_version": 1,
        "summary_style": "讲义式要点，每题约3-6条；只根据题目、答案、转写整理。",
        "entries": entries,
        "audio_without_transcript": transcript_data["audio_without_transcript"],
    }


def write_alignment_review(questions: list[Question], alignment_data: dict[str, Any]) -> None:
    q_by_id = {question.id: question for question in questions}
    lines: list[str] = [
        "# 讲解转写对齐复核清单",
        "",
        "说明：重点复核 low / medium、跨题、极短转写。确认后修改 review_summaries.json。",
        "",
    ]
    for alignment in alignment_data["alignments"]:
        if (
            alignment["confidence"] == "high"
            and not alignment["is_short"]
            and len(alignment["selected_question_ids"]) == 1
        ):
            continue
        lines.append(f"## {alignment['r_id']} ({alignment['confidence']})")
        lines.append("")
        lines.append(f"- 转写：`{alignment['transcript_path']}`")
        lines.append(f"- 录音：`{alignment['audio_path'] or '缺失'}`")
        lines.append(f"- 原因：{alignment['confidence_reason']}")
        lines.append(f"- 页码提示：{alignment['page_hints'] or '无'}；题号提示：{alignment['question_hints'] or '无'}")
        lines.append(f"- 摘录：{alignment['excerpt']}")
        lines.append("")
        lines.append("候选题：")
        for candidate in alignment["candidates"][:5]:
            question = q_by_id.get(candidate["question_id"])
            title = question.title if question else candidate["title"]
            lines.append(
                f"- `{candidate['question_id']}` P{candidate['page']} "
                f"{candidate['chapter']} 第{candidate['question_index']}题 "
                f"score={candidate['score']}: {title}"
            )
            lines.append(f"  - 证据：{'; '.join(candidate['reasons'][:3])}")
        lines.append("")
    REVIEW_MD.write_text("\n".join(lines).rstrip() + "\n", encoding="utf-8")


def prepare(args: argparse.Namespace) -> int:
    WORK_DIR.mkdir(exist_ok=True)
    questions = parse_questions()
    transcript_data = scan_transcripts()
    alignment_data = build_alignment(questions, transcript_data)

    write_json(
        QUESTIONS_JSON,
        {
            "source": rel(SOURCE_MD),
            "question_count": len(questions),
            "questions": [question.to_json() for question in questions],
        },
    )
    write_json(TRANSCRIPTS_JSON, transcript_data)
    write_json(ALIGNMENT_JSON, alignment_data)
    if args.force_review or not REVIEW_JSON.exists():
        write_json(REVIEW_JSON, make_review_summaries(questions, transcript_data, alignment_data))
    else:
        print(f"Keeping existing manual review file: {rel(REVIEW_JSON)}")
    write_alignment_review(questions, alignment_data)

    print(f"Questions: {len(questions)}")
    print(f"Audio files: {transcript_data['audio_count']}")
    print(f"Transcript files: {transcript_data['transcript_count']}")
    print(f"Confidence: {alignment_data['confidence_counts']}")
    print(f"Wrote {rel(WORK_DIR)}")
    return 0


def load_questions_from_work() -> list[Question]:
    data = read_json(QUESTIONS_JSON)
    questions = []
    for row in data["questions"]:
        questions.append(
            Question(
                id=row["id"],
                chapter_index=row["chapter_index"],
                chapter_title=row["chapter_title"],
                question_index=row["question_index"],
                title=row["title"],
                page=row["page"],
                start_line=row["start_line"],
                end_line=row["end_line"],
                block="",
                question_text=row["question_text"],
                answer_text=row["answer_text"],
                match_text=row["match_text"],
                keywords=row["keywords"],
            )
        )
    return questions


def apply_manual_review(args: argparse.Namespace) -> int:
    del args
    ensure_work_files()
    review_data = read_json(REVIEW_JSON)
    questions = load_questions_from_work()
    q_by_id = {question.id: question for question in questions}

    confirmed_count = 0
    unconfirmed_count = 0
    for entry in review_data["entries"]:
        r_id = entry["r_id"]
        if r_id in MANUAL_UNCONFIRMED:
            entry["status"] = "unconfirmed"
            entry["review_required"] = True
            entry["unconfirmed_reason"] = MANUAL_UNCONFIRMED[r_id]
            entry["student_ready_status"] = "skipped"
            entry["student_not_ready_reason"] = MANUAL_UNCONFIRMED[r_id]
            for note in entry.get("question_notes", []):
                note["status"] = "unconfirmed"
                note["review_notes"] = MANUAL_UNCONFIRMED[r_id]
                note["student_ready_status"] = "skipped"
                note["student_not_ready_reason"] = MANUAL_UNCONFIRMED[r_id]
            unconfirmed_count += 1
            continue

        selected_ids = MANUAL_SELECTED_IDS.get(r_id, entry.get("selected_question_ids", []))
        transcript_text = read_text(ROOT / entry["transcript_path"])
        question_notes: list[dict[str, Any]] = []
        for qid in selected_ids:
            question = q_by_id.get(qid)
            if question is None:
                raise ValueError(f"{r_id} references unknown question id: {qid}")
            manual_bullets = MANUAL_BULLETS.get((r_id, qid))
            bullets = manual_bullets or extractive_bullets(transcript_text, question)
            if not bullets:
                continue
            review_note = "人工复核：转写主题、题干关键词或文件名线索与本题一致。"
            student_ready_status = "skipped"
            student_ready_reason = ""
            if manual_bullets:
                review_note = "人工复核：跨题转写已按题干和主题词切分。"
                student_ready_status = "ready"
                student_ready_reason = "手工复核讲义要点，已按题干和主题词切分。"
            elif entry.get("confidence") == "high":
                student_ready_status = "ready"
                student_ready_reason = "高置信度来源，正文采用安全讲义模板生成。"
            else:
                student_ready_reason = "严格可靠策略：非高置信度且无手工讲义要点，移入附录供追溯。"
            question_notes.append(
                {
                    "question_id": qid,
                    "status": "confirmed",
                    "summary_bullets": bullets,
                    "review_notes": review_note,
                    "student_ready_status": student_ready_status,
                    "student_ready_reason": student_ready_reason,
                }
            )

        if question_notes:
            entry["status"] = "confirmed"
            entry["review_required"] = False
            entry["selected_question_ids"] = selected_ids
            entry["question_notes"] = question_notes
            entry["unconfirmed_reason"] = ""
            if any(note["student_ready_status"] == "ready" for note in question_notes):
                entry["student_ready_status"] = "ready"
                entry["student_not_ready_reason"] = ""
            else:
                entry["student_ready_status"] = "skipped"
                entry["student_not_ready_reason"] = "严格可靠策略：没有可进入正文的可靠讲解。"
            confirmed_count += 1
        else:
            entry["status"] = "unconfirmed"
            entry["review_required"] = True
            entry["question_notes"] = []
            entry["unconfirmed_reason"] = "人工复核后未生成可用讲解要点。"
            entry["student_ready_status"] = "skipped"
            entry["student_not_ready_reason"] = entry["unconfirmed_reason"]
            unconfirmed_count += 1

    notes_by_q, unconfirmed_entries, _ = build_notes_by_question(review_data)
    review_data["manual_review"] = {
        "status": "applied",
        "confirmed_entries": confirmed_count,
        "unconfirmed_entries": unconfirmed_count,
        "confirmed_question_count": len(notes_by_q),
        "unconfirmed_r_ids": [entry["r_id"] for entry in unconfirmed_entries],
    }
    write_json(REVIEW_JSON, review_data)

    print(f"Wrote {rel(REVIEW_JSON)}")
    print(f"Confirmed transcript entries: {confirmed_count}")
    print(f"Unconfirmed transcript entries: {unconfirmed_count}")
    print(f"Questions with confirmed lecture notes: {len(notes_by_q)}")
    return 0


def build_notes_by_question(
    review_data: dict[str, Any],
) -> tuple[dict[str, list[dict[str, Any]]], list[dict[str, Any]], dict[str, list[dict[str, Any]]]]:
    notes_by_q: dict[str, list[dict[str, Any]]] = defaultdict(list)
    appendix_entries: list[dict[str, Any]] = []
    skipped_by_q: dict[str, list[dict[str, Any]]] = defaultdict(list)
    for entry in review_data["entries"]:
        entry_status = entry.get("status", "needs_review")
        if entry_status != "confirmed":
            appendix_entries.append(entry)
            for qid in entry.get("selected_question_ids", []):
                skipped_by_q[qid].append(entry)
            continue
        confirmed_any = False
        skipped_any = False
        for note in entry.get("question_notes", []):
            if note.get("status", entry_status) != "confirmed":
                skipped_any = True
                skipped_by_q[note.get("question_id", "")].append(entry)
                continue
            if note.get("student_ready_status") != "ready":
                skipped_any = True
                skipped_by_q[note.get("question_id", "")].append(entry)
                continue
            bullets = [bullet.strip() for bullet in note.get("summary_bullets", []) if bullet.strip()]
            if not bullets:
                skipped_any = True
                skipped_by_q[note.get("question_id", "")].append(entry)
                continue
            confirmed_any = True
            notes_by_q[note["question_id"]].append(
                {
                    "r_id": entry["r_id"],
                    "transcript_path": entry["transcript_path"],
                    "audio_path": entry.get("audio_path", ""),
                    "confidence": entry.get("confidence", ""),
                    "review_notes": note.get("review_notes", ""),
                    "student_ready_reason": note.get("student_ready_reason", ""),
                    "bullets": bullets,
                }
            )
        if not confirmed_any:
            appendix_entries.append(entry)
        elif skipped_any:
            entry_copy = dict(entry)
            entry_copy["student_not_ready_reason"] = "部分讲解片段未达到学生版正文采用标准。"
            appendix_entries.append(entry_copy)
    skipped_by_q.pop("", None)
    return notes_by_q, appendix_entries, skipped_by_q


def merged_bullets(notes: list[dict[str, Any]]) -> list[str]:
    by_prefix: dict[str, str] = {}
    extras: list[str] = []
    for note in notes:
        for bullet in note["bullets"]:
            bullet = bullet.strip()
            if not bullet:
                continue
            prefix = bullet.split("：", 1)[0] if "：" in bullet else ""
            if prefix in LECTURE_PREFIXES:
                by_prefix.setdefault(prefix, bullet)
            elif bullet not in extras:
                extras.append(bullet)
    merged = [by_prefix[prefix] for prefix in LECTURE_PREFIXES if prefix in by_prefix]
    for bullet in extras:
        if len(merged) >= 6:
            break
        merged.append(bullet)
    return merged


def render_source_summary(notes: list[dict[str, Any]]) -> str:
    parts: list[str] = []
    for note in notes:
        transcript_name = Path(note["transcript_path"]).name
        audio_name = Path(note["audio_path"]).name if note.get("audio_path") else "缺失录音"
        confidence = note.get("confidence", "")
        parts.append(f"`{transcript_name}`（录音：`{audio_name}`，置信度：{confidence}）")
    return "；".join(parts)


def render_lecture_section(
    question: Question,
    notes: list[dict[str, Any]],
    skipped_sources: list[dict[str, Any]],
) -> str:
    lines = ["### 🎧 讲解"]
    if not notes:
        lines.append("")
        if skipped_sources:
            lines.append("*暂无可靠讲解来源；已有转写因匹配或可读性风险移入文末附录。*")
        else:
            lines.append("*暂无可靠讲解来源。*")
        return "\n".join(lines)

    lines.append("")
    lecture_style = "课堂思路版提炼" if question.id in DETAILED_LECTURE_BULLETS else "讲义式提炼"
    lines.append(
        f"> 来源：{render_source_summary(notes)}；复核状态：学生版已采用；整理方式：{lecture_style}。"
    )
    reasons = sorted({note.get("student_ready_reason", "") for note in notes if note.get("student_ready_reason")})
    if reasons:
        lines.append(f"> 采用依据：{'；'.join(reasons)}")
    lines.append("")
    for bullet in DETAILED_LECTURE_BULLETS.get(question.id, merged_bullets(notes)):
        lines.append(f"- {bullet}")
    return "\n".join(lines)


def insert_lecture_sections(
    notes_by_q: dict[str, list[dict[str, Any]]],
    skipped_by_q: dict[str, list[dict[str, Any]]],
) -> str:
    questions = parse_questions()
    output: list[str] = []
    current_chapter_index: int | None = None
    has_rendered_question = False

    for question in questions:
        notes = notes_by_q.get(question.id, [])
        if not notes:
            continue

        if has_rendered_question:
            output.extend(["", "---", ""])

        if question.chapter_index != current_chapter_index:
            current_chapter_index = question.chapter_index
            output.append(f"# {question.chapter_title}")
            output.append("")

        block_lines = question.block.splitlines()
        insert_at = len(block_lines)
        for idx, block_line in enumerate(block_lines):
            if block_line.strip() == "---":
                insert_at = idx
                break
        before = block_lines[:insert_at]
        while before and not before[-1].strip():
            before.pop()
        output.extend(before)
        output.append("")
        output.extend(
            render_lecture_section(
                question,
                notes,
                skipped_by_q.get(question.id, []),
            ).splitlines()
        )
        has_rendered_question = True
    return "\n".join(output).rstrip() + "\n"


def render_appendix(appendix_entries: list[dict[str, Any]], review_data: dict[str, Any]) -> str:
    lines = [
        "",
        "---",
        "",
        "# 附录：未采用讲解来源",
        "",
        "以下转写未插入题目正文，原因是匹配证据不足、内容过短、跨题无法可靠切分，或按学生版严格可靠策略不宜作为正文讲解。",
        "",
    ]
    if not appendix_entries:
        lines.append("无。")
    else:
        seen_r_ids: set[str] = set()
        for entry in appendix_entries:
            if entry["r_id"] in seen_r_ids:
                continue
            seen_r_ids.add(entry["r_id"])
            selected = "、".join(entry.get("selected_question_ids", [])) or "无"
            reason = (
                entry.get("student_not_ready_reason")
                or entry.get("unconfirmed_reason")
                or entry.get("status", "未确认")
            )
            lines.append(f"## {entry['r_id']}")
            lines.append("")
            lines.append(f"- 转写：`{entry['transcript_path']}`")
            lines.append(f"- 录音：`{entry.get('audio_path') or '缺失'}`")
            lines.append(f"- 自动候选：{selected}")
            lines.append(f"- 未插入原因：{reason}")
            lines.append("")

    missing = review_data.get("audio_without_transcript", [])
    lines.extend(["", "# 附录：缺少转写的录音", ""])
    if not missing:
        lines.append("无。")
    else:
        for item in missing:
            lines.append(f"- `{item['audio_path']}`：未发现对应 R 编号转写。")
    return "\n".join(lines).rstrip() + "\n"


def write_student_quality_report(
    review_data: dict[str, Any],
    notes_by_q: dict[str, list[dict[str, Any]]],
    appendix_entries: list[dict[str, Any]],
    skipped_by_q: dict[str, list[dict[str, Any]]],
) -> None:
    questions = load_questions_from_work()
    ready_source_count = sum(len(notes) for notes in notes_by_q.values())
    appendix_r_ids = list(dict.fromkeys(entry["r_id"] for entry in appendix_entries))
    lines = [
        "# 学生版讲解质量报告",
        "",
        "## 汇总",
        "",
        f"- 正文采用讲解来源：{ready_source_count}",
        f"- 正文覆盖题目：{len(notes_by_q)} / {len(questions)}",
        f"- 移入附录转写：{len(appendix_r_ids)}",
        f"- 原始复核条目：{len(review_data.get('entries', []))}",
        "",
        "## 移入附录的转写",
        "",
    ]
    if appendix_entries:
        for entry in appendix_entries:
            reason = (
                entry.get("student_not_ready_reason")
                or entry.get("unconfirmed_reason")
                or entry.get("status", "未确认")
            )
            selected = "、".join(entry.get("selected_question_ids", [])) or "无"
            lines.append(f"- `{entry['r_id']}` -> {selected}：{reason}")
    else:
        lines.append("- 无。")

    lines.extend(["", "## 暂无可靠讲解的题目", ""])
    missing = [question for question in questions if question.id not in notes_by_q]
    if not missing:
        lines.append("- 无。")
    else:
        current_chapter = ""
        for question in missing:
            if question.chapter_title != current_chapter:
                current_chapter = question.chapter_title
                lines.append(f"### {current_chapter}")
                lines.append("")
            skipped_count = len(skipped_by_q.get(question.id, []))
            suffix = "；有转写移入附录" if skipped_count else "；未匹配到可靠转写"
            lines.append(f"- `{question.id}` 第{question.question_index}题：{question_prompt(question)}{suffix}")

    STUDENT_REPORT_MD.write_text("\n".join(lines).rstrip() + "\n", encoding="utf-8")


def render(args: argparse.Namespace) -> int:
    del args
    ensure_work_files()
    review_data = read_json(REVIEW_JSON)
    notes_by_q, appendix_entries, skipped_by_q = build_notes_by_question(review_data)
    body = insert_lecture_sections(notes_by_q, skipped_by_q)
    body += render_appendix(appendix_entries, review_data)
    OUTPUT_MD.write_text(body, encoding="utf-8")
    write_student_quality_report(review_data, notes_by_q, appendix_entries, skipped_by_q)
    print(f"Wrote {rel(OUTPUT_MD)}")
    print(f"Wrote {rel(STUDENT_REPORT_MD)}")
    print(f"Questions with reliable lecture notes: {len(notes_by_q)}")
    print(f"Transcript entries in appendix: {len(dict.fromkeys(entry['r_id'] for entry in appendix_entries))}")
    return 0


def ensure_work_files() -> None:
    missing = [path for path in (QUESTIONS_JSON, TRANSCRIPTS_JSON, ALIGNMENT_JSON, REVIEW_JSON) if not path.exists()]
    if missing:
        names = ", ".join(rel(path) for path in missing)
        raise FileNotFoundError(f"Missing work files: {names}. Run prepare first.")


def check(args: argparse.Namespace) -> int:
    del args
    ensure_work_files()
    if not OUTPUT_MD.exists():
        raise FileNotFoundError(f"Missing output Markdown: {rel(OUTPUT_MD)}. Run render first.")

    questions = parse_questions()
    review_data = read_json(REVIEW_JSON)
    notes_by_q, appendix_entries, _ = build_notes_by_question(review_data)
    output = read_text(OUTPUT_MD)
    errors: list[str] = []
    lecture_blocks = re.findall(r"### 🎧 讲解\n(.*?)(?=\n---\n|\Z)", output, flags=re.S)
    lecture_text = "\n".join(lecture_blocks)
    expected_rendered_questions = len(notes_by_q)

    if len(questions) != 115:
        errors.append(f"source question count expected 115, got {len(questions)}")
    if output.count("### 🎧 讲解") != expected_rendered_questions:
        errors.append(
            "lecture section count expected "
            f"{expected_rendered_questions}, got {output.count('### 🎧 讲解')}"
        )
    if output.count("### 📘 题目与材料") != expected_rendered_questions:
        errors.append(
            "question/material section count expected "
            f"{expected_rendered_questions}, got {output.count('### 📘 题目与材料')}"
        )
    if output.count("### ✍️ 参考答案") != expected_rendered_questions:
        errors.append(
            "answer section count expected "
            f"{expected_rendered_questions}, got {output.count('### ✍️ 参考答案')}"
        )
    if "TODO" in output:
        errors.append("output contains TODO")
    if re.search(r"来源：``|录音：``", output):
        errors.append("output contains empty source code span")
    if "暂无可靠讲解来源" in output:
        errors.append("output contains missing lecture placeholder")
    if "暂无确认讲解" in lecture_text:
        errors.append("lecture placeholder still uses old unconfirmed wording")
    if "本题要处理的是：。" in lecture_text:
        errors.append("lecture contains empty question prompt")
    if "转写中特别提示" in lecture_text:
        errors.append("lecture contains raw transcript prompt wording")
    if "..." in lecture_text:
        errors.append("lecture contains ASCII truncation ellipsis")
    for term in ASR_NOISE_TERMS:
        if term in lecture_text:
            errors.append(f"lecture contains likely ASR noise term: {term}")

    seen_entries: set[str] = set()
    for entry in review_data["entries"]:
        r_id = entry["r_id"]
        if r_id in seen_entries:
            errors.append(f"duplicate review entry: {r_id}")
        seen_entries.add(r_id)
        transcript_path = ROOT / entry["transcript_path"]
        if not transcript_path.exists():
            errors.append(f"missing transcript source: {entry['transcript_path']}")
        audio_path = entry.get("audio_path")
        if audio_path and not (ROOT / audio_path).exists():
            errors.append(f"missing audio source: {audio_path}")

    for qid, notes in notes_by_q.items():
        if not notes:
            continue
        if qid not in {question.id for question in questions}:
            errors.append(f"review references unknown question id: {qid}")
        for note in notes:
            if note.get("confidence") != "high" and not note.get("student_ready_reason", "").startswith("手工复核"):
                errors.append(f"non-high source used without manual review: {note['r_id']} -> {qid}")

    for entry in appendix_entries:
        if entry["r_id"] not in output:
            errors.append(f"appendix transcript missing from output: {entry['r_id']}")

    known_missing = {item["r_id"] for item in review_data.get("audio_without_transcript", [])}
    expected_missing = {"R20240625-014512", "R20240626-064536"}
    unexpected_missing = known_missing - expected_missing
    if unexpected_missing:
        errors.append(f"unexpected missing transcript audio ids: {sorted(unexpected_missing)}")

    if errors:
        print("Check failed:")
        for error in errors:
            print(f"- {error}")
        return 1

    print("Check passed.")
    print(f"Reliable lecture questions: {len(notes_by_q)}")
    print(f"Transcript entries in appendix: {len(dict.fromkeys(entry['r_id'] for entry in appendix_entries))}")
    return 0


def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description=__doc__)
    subparsers = parser.add_subparsers(dest="command", required=True)

    prepare_parser = subparsers.add_parser("prepare", help="generate work files")
    prepare_parser.add_argument(
        "--force-review",
        action="store_true",
        help="overwrite review_summaries.json with a fresh scaffold",
    )
    prepare_parser.set_defaults(func=prepare)

    review_parser = subparsers.add_parser("apply-review", help="apply built-in manual review decisions")
    review_parser.set_defaults(func=apply_manual_review)

    render_parser = subparsers.add_parser("render", help="render final Markdown")
    render_parser.set_defaults(func=render)

    check_parser = subparsers.add_parser("check", help="validate final Markdown")
    check_parser.set_defaults(func=check)
    return parser


def main(argv: list[str] | None = None) -> int:
    parser = build_parser()
    args = parser.parse_args(argv)
    try:
        return args.func(args)
    except Exception as exc:  # noqa: BLE001 - CLI should show concise failures.
        print(f"error: {exc}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
